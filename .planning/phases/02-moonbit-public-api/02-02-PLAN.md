---
phase: 02-moonbit-public-api
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - jqx.mbt
  - jqx_public_types.mbt
  - jqx_test.mbt
  - README.mbt.md
autonomous: true
requirements:
  - MBT-02
  - MBT-03
user_setup: []
must_haves:
  truths:
    - "Compiled filters are a clear reuse-oriented public abstraction rather than an internal-core leak."
    - "The value lane and JSON-text compatibility lane stay explicit and separate for compiled execution."
    - "MoonBit users can rely on compiled-filter methods that match the direct runtime semantics without obscuring which lane preserves jq-style text fidelity."
  artifacts:
    - path: "jqx.mbt"
      provides: "CompiledFilter methods and lane-separated top-level runtime hooks"
      min_lines: 40
    - path: "jqx_public_types.mbt"
      provides: "Opaque compiled-filter boundary and any supporting public method types"
      min_lines: 40
    - path: "jqx_test.mbt"
      provides: "Compiled execution proof across value and text lanes"
      min_lines: 40
    - path: "README.mbt.md"
      provides: "Compiled-filter usage docs with clear lane ordering"
      min_lines: 40
  key_links:
    - from: "jqx.mbt"
      to: "core/jqx.mbt"
      via: "compiled execution delegates into shared core helpers rather than wrapper-local semantics"
      pattern: "run_compiled_json_text|execute"
    - from: "jqx_test.mbt"
      to: "jqx.mbt"
      via: "top-level tests exercise `CompiledFilter::run` and `CompiledFilter::run_json_text`"
      pattern: "filter\\.run|filter\\.run_json_text"
    - from: "README.mbt.md"
      to: "jqx.mbt"
      via: "compiled examples document the value-lane and compatibility-lane separation"
      pattern: "CompiledFilter|run_json_text"
---

<objective>
Harden compiled-filter methods and lane separation.

Purpose: Make compiled execution clearly reusable, clearly public, and clearly split between `Json` and JSON-text responsibilities.
Output: A sharper compiled-filter abstraction with explicit value-lane and compatibility-lane proof.
</objective>

<execution_context>
@C:/Users/shina/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-moonbit-public-api/02-CONTEXT.md
@.planning/phases/02-moonbit-public-api/02-RESEARCH.md
@.planning/phases/02-moonbit-public-api/02-01-PLAN.md
@AGENTS.md

@jqx.mbt
@jqx_public_types.mbt
@jqx_test.mbt
@README.mbt.md
@core/jqx.mbt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit compiled-filter semantics against the Phase 2 lane rules</name>
  <files>jqx.mbt, jqx_public_types.mbt</files>
  <action>Review `CompiledFilter::run(...)` and `CompiledFilter::run_json_text(...)` against the locked decisions from CONTEXT.md. Ensure the compiled abstraction reads as the reuse path over the same top-level runtime contract, keeps the value lane on standard `Json`, and keeps the compatibility lane text-based. Avoid helper shapes that blur or invert those responsibilities.</action>
  <verify>moon check -d</verify>
  <done>The compiled-filter methods cleanly reflect the intended lane separation and reuse story.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression proof for compiled value-lane and text-lane behavior</name>
  <files>jqx_test.mbt</files>
  <action>Expand or tighten top-level tests so compiled execution proves both reuse and lane behavior: `CompiledFilter::run(Json)` for ordinary MoonBit embedding, and `CompiledFilter::run_json_text(...)` for jq-style fidelity-sensitive execution. Cover repr-sensitive outputs and key-order-sensitive outputs through the compiled path, not only direct execution.</action>
  <verify>moon test</verify>
  <done>Compiled execution is regression-protected across both public lanes.</done>
</task>

<task type="auto">
  <name>Task 3: Align MoonBit docs with the compiled-filter reuse path</name>
  <files>README.mbt.md, jqx.mbt</files>
  <action>Update compiled examples and method docs so they present `compile(...).run(...)` as the explicit reuse-oriented next step after direct `run(...)`, and `run_json_text(...)` as the fidelity-preserving companion lane rather than a fallback or implementation detail.</action>
  <verify>moon check -d && moon test</verify>
  <done>Compiled-filter docs teach the right role and lane ordering without competing with the direct runtime happy path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `moon check -d` succeeds
- [ ] `moon test` succeeds
- [ ] Top-level tests prove both compiled public lanes
- [ ] Docs present compiled execution as the reuse path and text execution as the formal compatibility lane
</verification>

<success_criteria>

- All tasks completed
- Compiled-filter methods are clear, public, and lane-separated
- Compiled execution is regression-protected through top-level MoonBit tests
- Docs explain compiled reuse without competing with the direct `Json` lane
  </success_criteria>

<output>
After completion, create `.planning/phases/02-moonbit-public-api/02-02-SUMMARY.md`
</output>

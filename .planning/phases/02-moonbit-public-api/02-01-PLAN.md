---
phase: 02-moonbit-public-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - jqx.mbt
  - README.mbt.md
  - jqx_test.mbt
autonomous: true
requirements:
  - ARCH-02
  - MBT-01
  - MBT-05
user_setup: []
must_haves:
  truths:
    - "MoonBit users see one small canonical top-level runtime API centered on `parse_json`, `is_valid_json`, `compile`, `run`, and `run_json_text`."
    - "The standard entrypoint is clearly `run(filter, input : Json)`, with compiled execution presented as the next step rather than a competing first path."
    - "Pre-1.0 cleanup can rename or remove weaker top-level API shapes without leaving alias-heavy debt behind."
  artifacts:
    - path: "jqx.mbt"
      provides: "Canonical top-level MoonBit runtime entrypoints"
      min_lines: 40
    - path: "README.mbt.md"
      provides: "MoonBit-facing docs aligned to the canonical entrypoint order"
      min_lines: 40
    - path: "jqx_test.mbt"
      provides: "Top-level tests for direct MoonBit runtime usage"
      min_lines: 40
    - path: "pkg.generated.mbti"
      provides: "Generated public surface proving the canonical runtime contract"
      min_lines: 20
  key_links:
    - from: "jqx.mbt"
      to: "pkg.generated.mbti"
      via: "top-level MoonBit exports appear in the generated public contract"
      pattern: "parse_json|is_valid_json|compile|run|run_json_text"
    - from: "README.mbt.md"
      to: "jqx.mbt"
      via: "MoonBit examples demonstrate the canonical top-level runtime functions"
      pattern: "run\\(|compile\\(|run_json_text"
    - from: "jqx_test.mbt"
      to: "jqx.mbt"
      via: "top-level tests exercise the public runtime API rather than core-only helpers"
      pattern: "run\\(|compile\\(|parse_json"
---

<objective>
Canonicalize the MoonBit top-level runtime entrypoints and naming.

Purpose: Make the top-level MoonBit package feel like one obvious API rather than a carry-over wrapper surface.
Output: A small, canonical runtime API and examples/tests that teach the right entrypoint order.
</objective>

<execution_context>
@C:/Users/shina/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-moonbit-public-api/02-CONTEXT.md
@.planning/phases/02-moonbit-public-api/02-RESEARCH.md
@AGENTS.md

@jqx.mbt
@jqx_test.mbt
@README.mbt.md
@pkg.generated.mbti
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and normalize the top-level runtime contract</name>
  <files>jqx.mbt, pkg.generated.mbti</files>
  <action>Review the current top-level MoonBit exports against the Phase 2 context and requirements. Remove, rename, or reorder public-facing entrypoints as needed so `parse_json`, `is_valid_json`, `compile`, `run`, and `run_json_text` are the one obvious top-level operations. Avoid introducing aliases for old shapes once the canonical contract is chosen.</action>
  <verify>moon info && moon check -d</verify>
  <done>The source and generated public surface agree on one small top-level runtime API with the intended canonical names.</done>
</task>

<task type="auto">
  <name>Task 2: Reframe docs and examples around the direct `Json` lane</name>
  <files>README.mbt.md, jqx.mbt</files>
  <action>Update MoonBit-facing docs and any top-level doc comments so the first normal workflow is `run(filter, input : Json)`. Keep `parse_json` and `is_valid_json` as supporting helpers, and present compiled execution only after the direct value-lane path is established. Do not route normal users through `core`.</action>
  <verify>moon check -d</verify>
  <done>MoonBit examples and top-level docs teach the direct `Json` lane first and clearly position compile as a follow-on reuse path.</done>
</task>

<task type="auto">
  <name>Task 3: Add top-level regression proof for the canonical entrypoint order</name>
  <files>jqx_test.mbt, README.mbt.md</files>
  <action>Add or refine top-level tests so they prove the expected direct-use flow through the public package: parsing to standard `Json`, validating JSON text, running filters directly on `Json`, and compiling filters through the same top-level package. Keep the tests focused on the observable public boundary rather than duplicating core-only checks.</action>
  <verify>moon test</verify>
  <done>Top-level tests fail if the canonical entrypoint order or basic runtime contract drifts away from the Phase 2 decisions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `moon info` succeeds
- [ ] `moon check -d` succeeds
- [ ] `moon test` succeeds
- [ ] `pkg.generated.mbti` reflects the intended canonical runtime surface with no alias-heavy drift
</verification>

<success_criteria>

- All tasks completed
- The top-level MoonBit package has one small canonical runtime surface
- Docs teach `run(filter, Json)` as the default entrypoint
- Generated API shape and tests prove the naming cleanup
  </success_criteria>

<output>
After completion, create `.planning/phases/02-moonbit-public-api/02-01-SUMMARY.md`
</output>

---
phase: 02-moonbit-public-api
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - jqx.mbt
  - jqx_public_types.mbt
  - jqx_test.mbt
  - README.mbt.md
autonomous: true
requirements:
  - ARCH-02
  - MBT-04
  - MBT-05
user_setup: []
must_haves:
  truths:
    - "Normal MoonBit users can consume `shina1024/jqx` without depending on `@core.Value` or `@core.Filter`."
    - "Public error contracts describe only the failure modes that can actually happen on each entrypoint, while preserving structured leaf payloads."
    - "Any advanced conversion helpers or escape hatches remain clearly secondary to the standard `Json` lane and JSON-text compatibility lane."
  artifacts:
    - path: "jqx_public_types.mbt"
      provides: "Narrow public error types and opaque compiled-filter boundary"
      min_lines: 60
    - path: "jqx.mbt"
      provides: "Top-level runtime API free of internal-type leakage in normal use"
      min_lines: 40
    - path: "pkg.generated.mbti"
      provides: "Generated proof that the public package does not leak core-only types in normal consumption"
      min_lines: 20
    - path: "README.mbt.md"
      provides: "MoonBit docs that keep `core` and advanced helpers out of the normal path"
      min_lines: 40
  key_links:
    - from: "jqx_public_types.mbt"
      to: "pkg.generated.mbti"
      via: "public error and compiled-filter changes are reflected in the generated MoonBit contract"
      pattern: "RuntimeError|CompileError|JsonParseError|RunError|JsonTextRunError|CompiledJsonTextRunError|CompiledFilter"
    - from: "jqx.mbt"
      to: "jqx_public_types.mbt"
      via: "top-level runtime functions raise the narrowed public error contracts"
      pattern: "raise "
    - from: "README.mbt.md"
      to: "jqx.mbt"
      via: "MoonBit docs route users through the top-level package rather than `core`"
      pattern: "shina1024/jqx"
---

<objective>
Remove or isolate internal-type leakage from the MoonBit public boundary.

Purpose: Make the public package idiomatic and durable before 1.0 by demoting internal escape hatches and narrowing public errors.
Output: A MoonBit public surface that stays on `Json`, keeps `CompiledFilter` opaque, and avoids broad or core-type-leaking contracts.
</objective>

<execution_context>
@C:/Users/shina/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-moonbit-public-api/02-CONTEXT.md
@.planning/phases/02-moonbit-public-api/02-RESEARCH.md
@.planning/phases/02-moonbit-public-api/02-01-PLAN.md
@.planning/phases/02-moonbit-public-api/02-02-PLAN.md
@AGENTS.md

@jqx.mbt
@jqx_public_types.mbt
@jqx_test.mbt
@README.mbt.md
@pkg.generated.mbti
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace broad catch-all public errors with entrypoint-shaped contracts</name>
  <files>jqx_public_types.mbt, jqx.mbt, jqx_test.mbt</files>
  <action>Refactor the top-level public errors so each entrypoint raises only the failures that can actually occur on that path, while keeping `JsonParseError`, `CompileError`, and `RuntimeError` as the structured leaf types. Remove or isolate any broad `JqxError`-style contract that forces users to branch on impossible variants.</action>
  <verify>moon info && moon check -d && moon test</verify>
  <done>The public runtime surface uses narrower composite errors, and top-level tests prove the new error mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Isolate advanced conversion helpers from the normal MoonBit path</name>
  <files>jqx.mbt, jqx_public_types.mbt, README.mbt.md</files>
  <action>Implement the minimal advanced-helper strategy needed for `Json` <-> internal-value escape hatches, if such helpers are still necessary after the error cleanup. Keep those helpers clearly secondary in naming and docs, and make sure repr-sensitive guidance points users to `run_json_text(...)` first. If no public helper is needed, document that decision and keep the escape hatch internal.</action>
  <verify>moon check -d</verify>
  <done>Any remaining escape hatches are clearly advanced-only and do not replace the standard `Json` lane or text lane in docs.</done>
</task>

<task type="auto">
  <name>Task 3: Audit the generated public surface and docs for remaining core leakage</name>
  <files>pkg.generated.mbti, README.mbt.md, jqx_test.mbt</files>
  <action>Review the generated `.mbti`, README examples, and top-level tests to ensure the normal MoonBit user path stays on `shina1024/jqx` without requiring `@core.Value` or `@core.Filter`. Tighten docs and tests wherever core-only types or broad internal-facing contracts still leak through.</action>
  <verify>moon info && moon fmt --check && moon check -d && moon test</verify>
  <done>The generated public surface, docs, and tests all reinforce the intended top-level boundary and no longer normalize core-only usage as the default path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `moon info` succeeds
- [ ] `moon fmt --check` succeeds
- [ ] `moon check -d` succeeds
- [ ] `moon test` succeeds
- [ ] `pkg.generated.mbti` no longer leaks core-only types into normal public consumption paths
- [ ] README and tests keep users on `shina1024/jqx` rather than `core`
</verification>

<success_criteria>

- All tasks completed
- Normal MoonBit users no longer need core-only types to consume the public package
- Public error contracts are narrower and more truthful
- Advanced escape hatches stay secondary to the standard `Json` and JSON-text lanes
  </success_criteria>

<output>
After completion, create `.planning/phases/02-moonbit-public-api/02-03-SUMMARY.md`
</output>

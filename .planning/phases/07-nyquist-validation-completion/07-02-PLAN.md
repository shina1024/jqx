---
phase: 07-nyquist-validation-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-cli-workflow-parity/04-VALIDATION.md
  - .planning/phases/06-release-readiness-and-docs/06-VALIDATION.md
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Phase 04 and Phase 06 validation artifacts now match the current CLI and release-readiness proof rather than their earlier draft state."
    - "Both validation docs clearly distinguish repository-verified automation from operational maintainer steps that remain manual."
    - "Nyquist compliance for the CLI and release phases is based on the current jq differential proof, release audit ledger, and MoonBit package-content checks."
  artifacts:
    - path: ".planning/phases/04-cli-workflow-parity/04-VALIDATION.md"
      provides: "Current validation contract for CLI workflow parity"
      min_lines: 45
    - path: ".planning/phases/06-release-readiness-and-docs/06-VALIDATION.md"
      provides: "Current validation contract for release readiness and docs"
      min_lines: 60
  key_links:
    - from: ".planning/phases/04-cli-workflow-parity/04-VALIDATION.md"
      to: ".planning/phases/04-cli-workflow-parity/04-VERIFICATION.md"
      via: "Phase 04 validation rows and manual checks match the current CLI parity verification report"
      pattern: "jqx|node scripts/jq_diff\\.mjs|scripts/jq_exit_cases\\.json"
    - from: ".planning/phases/06-release-readiness-and-docs/06-VALIDATION.md"
      to: ".planning/phases/06-release-readiness-and-docs/06-VERIFICATION.md"
      via: "Phase 06 validation rows and manual checks match the current release-readiness verification report"
      pattern: "06-RELEASE-AUDIT\\.md|moon package --list --manifest-path moon\\.mod\\.json|moon login"
---

<objective>
Reconcile the CLI- and release-oriented validation artifacts with the repository's current proof state.

Purpose: Remove draft-state validation debt from the completed Phase 04 and Phase 06 artifacts.
Output: Updated Phase 04 and Phase 06 validation docs that are ready to be marked Nyquist-compliant.
</objective>

<execution_context>
@C:/Users/shina/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/07-nyquist-validation-completion/07-CONTEXT.md
@.planning/phases/07-nyquist-validation-completion/07-VALIDATION.md
@AGENTS.md

@.planning/phases/04-cli-workflow-parity/04-VALIDATION.md
@.planning/phases/04-cli-workflow-parity/04-VERIFICATION.md
@.planning/phases/06-release-readiness-and-docs/06-VALIDATION.md
@.planning/phases/06-release-readiness-and-docs/06-VERIFICATION.md
@.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize Phase 04 validation against the current CLI parity proof</name>
  <files>.planning/phases/04-cli-workflow-parity/04-VALIDATION.md</files>
  <read_first>.planning/phases/04-cli-workflow-parity/04-VALIDATION.md, .planning/phases/04-cli-workflow-parity/04-VERIFICATION.md, cmd/main_cli.mbt, cmd/main.mbt, cmd/main_wbtest.mbt, scripts/jq_compat_cases.json, scripts/jq_exit_cases.json, AGENTS.md</read_first>
  <action>Update `.planning/phases/04-cli-workflow-parity/04-VALIDATION.md` from draft status to the final retrospective state. Set the frontmatter to `status: complete` and `nyquist_compliant: true`, keep `wave_0_complete: true`, change every Phase 04 task row to `✅ green`, and finish the sign-off with `Approval: approved`. Preserve the existing CLI-focused manual-only checks, but make sure their wording matches the current verification report: `jqx` is the canonical command name and CLI semantics still route through shared compile and execute paths rather than wrapper-local patches.</action>
  <verify>moon info && moon fmt && moon check && moon test && node scripts/jq_diff.mjs && node scripts/jq_diff.mjs scripts/jq_exit_cases.json</verify>
  <acceptance_criteria>
    - `.planning/phases/04-cli-workflow-parity/04-VALIDATION.md` contains `status: complete`
    - `.planning/phases/04-cli-workflow-parity/04-VALIDATION.md` contains `wave_0_complete: true`
    - `.planning/phases/04-cli-workflow-parity/04-VALIDATION.md` contains `nyquist_compliant: true`
    - `.planning/phases/04-cli-workflow-parity/04-VALIDATION.md` contains `Approval: approved`
    - `.planning/phases/04-cli-workflow-parity/04-VALIDATION.md` contains `jqx`
    - `.planning/phases/04-cli-workflow-parity/04-VALIDATION.md` does not contain `⬜ pending`
  </acceptance_criteria>
  <done>Phase 04 validation is fully signed off and matches the current CLI parity evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize Phase 06 validation against the current release-readiness proof</name>
  <files>.planning/phases/06-release-readiness-and-docs/06-VALIDATION.md</files>
  <read_first>.planning/phases/06-release-readiness-and-docs/06-VALIDATION.md, .planning/phases/06-release-readiness-and-docs/06-VERIFICATION.md, .planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md, .github/workflows/release-npm.yml, .github/workflows/release-cli.yml, moon.mod.json, README.mbt.md, AGENTS.md</read_first>
  <action>Update `.planning/phases/06-release-readiness-and-docs/06-VALIDATION.md` so it reflects the current Phase 06 release-readiness proof rather than the earlier draft assumptions. Set the frontmatter to `status: complete`, `wave_0_complete: true`, and `nyquist_compliant: true` once the body is consistent. Change every Phase 06 task row to `✅ green`, replace the stale Wave 0 blockers with the current repository evidence: the tracked `06-RELEASE-AUDIT.md`, the `moon.mod.json` exclusions for `_bundle_tmp` and `_bundle_wasmgc`, and the dry-run workflow paths in `release-npm.yml` and `release-cli.yml`. Keep authenticated `moon publish --dry-run --manifest-path moon.mod.json` documented as a manual operational prerequisite tied to `moon login`, not as a locally required anonymous gate, then finish the sign-off with `Approval: approved`.</action>
  <verify>moon info && moon fmt && moon check && moon test && bash ./scripts/ts_packages.sh verify --frozen-lockfile && moon package --list --manifest-path moon.mod.json</verify>
  <acceptance_criteria>
    - `.planning/phases/06-release-readiness-and-docs/06-VALIDATION.md` contains `status: complete`
    - `.planning/phases/06-release-readiness-and-docs/06-VALIDATION.md` contains `wave_0_complete: true`
    - `.planning/phases/06-release-readiness-and-docs/06-VALIDATION.md` contains `nyquist_compliant: true`
    - `.planning/phases/06-release-readiness-and-docs/06-VALIDATION.md` contains `06-RELEASE-AUDIT.md`
    - `.planning/phases/06-release-readiness-and-docs/06-VALIDATION.md` contains `moon login`
    - `.planning/phases/06-release-readiness-and-docs/06-VALIDATION.md` contains `Approval: approved`
    - `.planning/phases/06-release-readiness-and-docs/06-VALIDATION.md` does not contain `❌ W0`
    - `.planning/phases/06-release-readiness-and-docs/06-VALIDATION.md` does not contain `⬜ pending`
  </acceptance_criteria>
  <done>Phase 06 validation now matches the current release-workflow, MoonBit package, and documentation proof without overstating manual publish steps.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `moon info && moon fmt && moon check && moon test && node scripts/jq_diff.mjs && node scripts/jq_diff.mjs scripts/jq_exit_cases.json` succeeds
- [ ] `moon info && moon fmt && moon check && moon test && bash ./scripts/ts_packages.sh verify --frozen-lockfile && moon package --list --manifest-path moon.mod.json` succeeds
- [ ] `04-VALIDATION.md` and `06-VALIDATION.md` both have `status: complete`, `wave_0_complete: true`, and `nyquist_compliant: true`
- [ ] Both validation docs end with `Approval: approved`
</verification>

<success_criteria>

- All tasks completed
- Phase 04 and Phase 06 validation docs now describe the current CLI and release-readiness proof state truthfully
- Manual publish prerequisites remain explicit instead of being smuggled into automated proof
- The milestone audit can treat both phases as Nyquist-compliant once this plan lands
  </success_criteria>

<output>
After completion, create `.planning/phases/07-nyquist-validation-completion/07-02-SUMMARY.md`
</output>

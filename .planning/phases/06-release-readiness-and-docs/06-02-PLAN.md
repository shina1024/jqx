---
phase: 06-release-readiness-and-docs
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - moon.mod.json
  - README.mbt.md
  - .planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md
autonomous: true
requirements:
  - MBT-06
user_setup: []
must_haves:
  truths:
    - "Maintainers can verify MoonBit package readiness through package metadata, actual packaged contents, and an authenticated publish preflight."
    - "The MoonBit package bundle excludes `_bundle_tmp` and `_bundle_wasmgc` unless they are intentionally justified and documented."
    - "MoonBit install and quick-start guidance matches the canonical public API in `pkg.generated.mbti` and treats `shina1024/jqx` as a first-class public package."
  artifacts:
    - path: "moon.mod.json"
      provides: "MoonBit package identity, metadata, and published readme binding"
      min_lines: 8
    - path: "README.mbt.md"
      provides: "Published MoonBit readme and canonical install/quick-start guidance"
      min_lines: 120
    - path: ".planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md"
      provides: "Recorded MoonBit package-content and publish-preflight evidence"
      min_lines: 110
  key_links:
    - from: "moon.mod.json"
      to: "README.mbt.md"
      via: "the MoonBit package metadata publishes the same readme users see on the package surface"
      pattern: "\"readme\": \"README\\.mbt\\.md\"|moon add shina1024/jqx"
    - from: "README.mbt.md"
      to: "pkg.generated.mbti"
      via: "MoonBit install and quick-start examples match the generated public API names"
      pattern: "compile|run|run_json_text|CompiledFilter::run_json_text"
    - from: ".planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md"
      to: "moon.mod.json"
      via: "the audit ledger records the exact package identity and publish preflight commands"
      pattern: "shina1024/jqx|moon publish --dry-run --manifest-path moon.mod.json|moon package --list --manifest-path moon.mod.json"
---

<objective>
Finalize MoonBit publication readiness and package metadata.

Purpose: Make `MBT-06` provable through actual MoonBit package inspection and documented publish-preflight steps instead of treating publication readiness as a vague future state.
Output: Clean MoonBit package metadata, an auditable publish bundle, and a MoonBit package section in the tracked release audit.
</objective>

<execution_context>
@C:/Users/shina/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-release-readiness-and-docs/06-CONTEXT.md
@.planning/phases/06-release-readiness-and-docs/06-RESEARCH.md
@.planning/phases/06-release-readiness-and-docs/06-VALIDATION.md
@.planning/phases/06-release-readiness-and-docs/06-01-PLAN.md
@AGENTS.md

@moon.mod.json
@README.mbt.md
@pkg.generated.mbti
@jqx.mbt
@jqx_public_types.mbt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten MoonBit package metadata and bundle contents for publication readiness</name>
  <files>moon.mod.json, README.mbt.md</files>
  <read_first>moon.mod.json, README.mbt.md, pkg.generated.mbti, .planning/phases/06-release-readiness-and-docs/06-RESEARCH.md, .planning/phases/06-release-readiness-and-docs/06-VALIDATION.md, AGENTS.md</read_first>
  <action>Keep the MoonBit package identity as `shina1024/jqx` and `readme` as `README.mbt.md`, then update `moon.mod.json` metadata and the published readme text so the package is described as a first-class public surface rather than a tentative future channel. Add an explicit MoonBit install command `moon add shina1024/jqx` to `README.mbt.md`, keep the quick-start centered on `run`, `compile`, and `run_json_text`, and make the package-content audit pass by ensuring `moon package --list --manifest-path moon.mod.json` no longer includes `_bundle_tmp` or `_bundle_wasmgc` unless the audit document records an explicit justification for keeping them.</action>
  <verify>moon package --list --manifest-path moon.mod.json</verify>
  <acceptance_criteria>
    - `moon.mod.json` contains `"name": "shina1024/jqx"`
    - `moon.mod.json` contains `"readme": "README.mbt.md"`
    - `README.mbt.md` contains `moon add shina1024/jqx`
    - `README.mbt.md` contains `run_json_text`
    - `README.mbt.md` does not contain `MoonBit package on mooncakes.io: planned`
    - `moon package --list --manifest-path moon.mod.json` exits 0
    - `moon package --list --manifest-path moon.mod.json` output does not contain `_bundle_tmp`
    - `moon package --list --manifest-path moon.mod.json` output does not contain `_bundle_wasmgc`
  </acceptance_criteria>
  <done>The MoonBit package metadata and bundle contents are ready for publication review.</done>
</task>

<task type="auto">
  <name>Task 2: Record authenticated MoonBit publish preflight requirements in the release audit ledger</name>
  <files>.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md, README.mbt.md</files>
  <read_first>.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md, moon.mod.json, README.mbt.md, pkg.generated.mbti, .planning/phases/06-release-readiness-and-docs/06-RESEARCH.md, .planning/phases/06-release-readiness-and-docs/06-VALIDATION.md</read_first>
  <action>Update the `## MoonBit Package Readiness` section in `.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md` to record the package identity `shina1024/jqx@0.1.0`, the readme path `README.mbt.md`, the package-content audit command `moon package --list --manifest-path moon.mod.json`, the authenticated publish-preflight command `moon publish --dry-run --manifest-path moon.mod.json`, and the prerequisite `moon login`. In `README.mbt.md`, make the MoonBit install and quick-start text agree with the same first-class package story and the generated API names from `pkg.generated.mbti`.</action>
  <verify>rg "shina1024/jqx@0.1.0|moon package --list --manifest-path moon.mod.json|moon publish --dry-run --manifest-path moon.mod.json|moon login|run_json_text" .planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md README.mbt.md</verify>
  <acceptance_criteria>
    - `.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md` contains `shina1024/jqx@0.1.0`
    - `.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md` contains `moon package --list --manifest-path moon.mod.json`
    - `.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md` contains `moon publish --dry-run --manifest-path moon.mod.json`
    - `.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md` contains `moon login`
    - `README.mbt.md` contains `moon add shina1024/jqx`
    - `README.mbt.md` contains `compile`
    - `README.mbt.md` contains `run_json_text`
  </acceptance_criteria>
  <done>The tracked release audit explains exactly how MoonBit publication readiness is proved, including the login prerequisite for dry-run publish.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `moon package --list --manifest-path moon.mod.json` succeeds
- [ ] package listing no longer includes `_bundle_tmp` or `_bundle_wasmgc`
- [ ] `README.mbt.md` presents `shina1024/jqx` as a first-class MoonBit package
- [ ] `.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md` records `moon login` plus `moon publish --dry-run --manifest-path moon.mod.json`
</verification>

<success_criteria>

- All tasks completed
- MoonBit package readiness is proven through metadata, bundle contents, and an authenticated publish-preflight path
- Root MoonBit docs use canonical API names and the real package install command
- The release audit records the exact steps a maintainer must run before publication
  </success_criteria>

<output>
After completion, create `.planning/phases/06-release-readiness-and-docs/06-02-SUMMARY.md`
</output>

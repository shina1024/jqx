---
phase: 06-release-readiness-and-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release-npm.yml
  - .github/workflows/release-cli.yml
  - .planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md
autonomous: true
requirements:
  - CLI-03
  - REL-01
  - REL-02
user_setup: []
must_haves:
  truths:
    - "Maintainers can run a non-publishing npm and CLI release proof path before the first public release."
    - "CLI release artifacts keep the public executable name `jqx` / `jqx.exe` inside the packaged archives."
    - "A repository-tracked Phase 6 audit document records workflow inputs, artifact names, package names, and smoke results for release readiness."
  artifacts:
    - path: ".github/workflows/release-npm.yml"
      provides: "npm dry-run workflow contract and per-package verification path"
      min_lines: 90
    - path: ".github/workflows/release-cli.yml"
      provides: "CLI multi-OS packaging contract with a non-publishing audit path"
      min_lines: 120
    - path: ".planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md"
      provides: "Repository-tracked release readiness evidence for npm, CLI, and MoonBit"
      min_lines: 80
  key_links:
    - from: ".github/workflows/release-npm.yml"
      to: "scripts/ts_packages.mjs"
      via: "npm release proof iterates the same publishable package list used by the repository tooling"
      pattern: "scripts/ts_packages\\.mjs|dry_run|verify --frozen-lockfile"
    - from: ".github/workflows/release-cli.yml"
      to: ".planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md"
      via: "CLI workflow asset names and archive expectations are recorded in the tracked audit document"
      pattern: "jqx-\\$\\{TAG_NAME\\}-linux\\.tar\\.gz|jqx-\\$\\{TAG_NAME\\}-macos\\.tar\\.gz|jqx-\\$env:TAG_NAME-windows\\.zip"
    - from: ".github/workflows/release-cli.yml"
      to: ".github/workflows/release-npm.yml"
      via: "both release surfaces expose a non-publishing proof path before any public publish step"
      pattern: "dry_run|workflow_dispatch|upload-artifact"
---

<objective>
Harden release workflows into auditable, non-publishing proof paths.

Purpose: Make npm and CLI release readiness reviewable before first publication by separating artifact inspection from public release side effects.
Output: Workflow updates plus a tracked release audit document that records artifact names, package names, and smoke-check evidence.
</objective>

<execution_context>
@C:/Users/shina/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-release-readiness-and-docs/06-CONTEXT.md
@.planning/phases/06-release-readiness-and-docs/06-RESEARCH.md
@.planning/phases/06-release-readiness-and-docs/06-VALIDATION.md
@AGENTS.md

@.github/workflows/release-npm.yml
@.github/workflows/release-cli.yml
@scripts/ts_packages.mjs
@scripts/ts_packages.sh
@ts/jqx/package.json
@moon.mod.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit non-publishing audit paths to the npm and CLI release workflows</name>
  <files>.github/workflows/release-npm.yml, .github/workflows/release-cli.yml</files>
  <read_first>.github/workflows/release-npm.yml, .github/workflows/release-cli.yml, scripts/ts_packages.mjs, scripts/ts_packages.sh, .planning/phases/06-release-readiness-and-docs/06-CONTEXT.md, .planning/phases/06-release-readiness-and-docs/06-RESEARCH.md, AGENTS.md</read_first>
  <action>Keep the npm workflow's `dry_run` input and the existing `bash ./scripts/ts_packages.sh verify --frozen-lockfile` gate, then add an explicit audit step that records each package returned by `node ./scripts/ts_packages.mjs list`, its `package.json` name and version, and the exported runtime entrypoints that will be published. In `release-cli.yml`, add a boolean `dry_run` input to `workflow_dispatch`, carry it through the `prepare` job outputs, keep the existing Linux/macOS/Windows packaging steps that copy the built executable to `dist/jqx` or `dist/jqx.exe`, keep archive names `jqx-${TAG_NAME}-linux.tar.gz`, `jqx-${TAG_NAME}-macos.tar.gz`, and `jqx-${TAG_NAME}-windows.zip`, upload packaged artifacts in all modes, and guard the `Publish GitHub Release` step so it does not run when `dry_run` is `true`.</action>
  <verify>rg "dry_run|upload-artifact|verify --frozen-lockfile|pnpm publish --access public --dry-run --no-git-checks|dist/jqx|dist/jqx\\.exe" .github/workflows/release-npm.yml .github/workflows/release-cli.yml</verify>
  <acceptance_criteria>
    - `.github/workflows/release-npm.yml` contains `dry_run`
    - `.github/workflows/release-npm.yml` contains `bash ./scripts/ts_packages.sh verify --frozen-lockfile`
    - `.github/workflows/release-npm.yml` contains `pnpm publish --access public --dry-run --no-git-checks`
    - `.github/workflows/release-cli.yml` contains `dry_run`
    - `.github/workflows/release-cli.yml` contains `dist/jqx`
    - `.github/workflows/release-cli.yml` contains `dist/jqx.exe`
    - `.github/workflows/release-cli.yml` contains `actions/upload-artifact`
    - `.github/workflows/release-cli.yml` contains `if: ${{ needs.prepare.outputs.dry_run != 'true' }}`
  </acceptance_criteria>
  <done>Both release workflows expose an auditable non-publishing path before public publication.</done>
</task>

<task type="auto">
  <name>Task 2: Create the tracked Phase 6 release audit ledger with concrete evidence fields</name>
  <files>.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md</files>
  <read_first>.planning/phases/06-release-readiness-and-docs/06-CONTEXT.md, .planning/phases/06-release-readiness-and-docs/06-RESEARCH.md, .planning/phases/06-release-readiness-and-docs/06-VALIDATION.md, .github/workflows/release-npm.yml, .github/workflows/release-cli.yml, scripts/ts_packages.mjs, ts/jqx/package.json, moon.mod.json</read_first>
  <action>Create `.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md` with the sections `## npm Dry Run`, `## CLI Packaging Dry Run`, and `## MoonBit Package Readiness`. In the npm section, pre-list the package names `@shina1024/jqx-adapter-core`, `@shina1024/jqx-zod-adapter`, `@shina1024/jqx-yup-adapter`, `@shina1024/jqx-valibot-adapter`, and `@shina1024/jqx`, plus fields for version, workflow run, `dist/` inspection, export-map review, and local consumer smoke results. Record the exact npm smoke commands `bash ./scripts/ts_packages.sh verify --frozen-lockfile`, `pnpm --dir ts/jqx test`, and package entrypoint checks for `@shina1024/jqx` and `@shina1024/jqx/bind`. In the CLI section, pre-list asset names `jqx-vX.Y.Z-linux.tar.gz`, `jqx-vX.Y.Z-macos.tar.gz`, and `jqx-vX.Y.Z-windows.zip`, plus fields for archive contents, extracted executable name, and smoke commands `jqx "." "null"` and `jqx.exe "." "null"`. In the MoonBit section, add placeholders for `moon package --list --manifest-path moon.mod.json`, `moon publish --dry-run --manifest-path moon.mod.json`, and the package identity `shina1024/jqx`.</action>
  <verify>rg "## npm Dry Run|## CLI Packaging Dry Run|## MoonBit Package Readiness|@shina1024/jqx-adapter-core|@shina1024/jqx/bind|bash ./scripts/ts_packages.sh verify --frozen-lockfile|pnpm --dir ts/jqx test|moon publish --dry-run --manifest-path moon.mod.json" .planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md</verify>
  <acceptance_criteria>
    - `.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md` contains `## npm Dry Run`
    - `.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md` contains `## CLI Packaging Dry Run`
    - `.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md` contains `## MoonBit Package Readiness`
    - `.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md` contains `@shina1024/jqx-adapter-core`
    - `.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md` contains `@shina1024/jqx`
    - `.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md` contains `@shina1024/jqx/bind`
    - `.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md` contains `bash ./scripts/ts_packages.sh verify --frozen-lockfile`
    - `.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md` contains `pnpm --dir ts/jqx test`
    - `.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md` contains `jqx-vX.Y.Z-linux.tar.gz`
    - `.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md` contains `jqx "." "null"`
    - `.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md` contains `moon publish --dry-run --manifest-path moon.mod.json`
  </acceptance_criteria>
  <done>The phase has a repository-tracked audit ledger that records concrete npm, CLI, and MoonBit release evidence.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.github/workflows/release-npm.yml` still verifies `bash ./scripts/ts_packages.sh verify --frozen-lockfile`
- [ ] `.github/workflows/release-cli.yml` supports a non-publishing `dry_run` path
- [ ] CLI workflow still packages `dist/jqx` and `dist/jqx.exe` into the release archives
- [ ] `.planning/phases/06-release-readiness-and-docs/06-RELEASE-AUDIT.md` exists and records npm, CLI, and MoonBit audit fields
</verification>

<success_criteria>

- All tasks completed
- npm and CLI release proof can run without public side effects
- CLI archives preserve `jqx` / `jqx.exe` as the public executable names
- Release readiness evidence is tracked in-repo instead of living only in CI logs
  </success_criteria>

<output>
After completion, create `.planning/phases/06-release-readiness-and-docs/06-01-SUMMARY.md`
</output>

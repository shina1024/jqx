---
phase: 01-shared-core-and-compatibility
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - core/execute_test.mbt
  - core/execute_test_support_test.mbt
  - js/jqx_js_test.mbt
  - jqx_test.mbt
  - scripts/jq_diff.sh
  - scripts/jq_diff_native.sh
  - .github/workflows/ci.yml
  - .github/workflows/release-cli.yml
autonomous: true
requirements:
  - COMP-03
  - COMP-04
user_setup: []
must_haves:
  truths:
    - "JSON-text fidelity and object-order-sensitive behavior are explicitly proven through MoonBit top-level tests, JS-target tests, and CLI differential harnesses."
    - "CI proves the repo's real Phase 1 quality gate without obvious redundant or missing checks."
    - "By the end of Phase 1, the green gate reflects shared-core parity rather than broad but low-signal automation."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Phase-appropriate CI proof surface"
      min_lines: 50
    - path: "scripts/jq_diff.sh"
      provides: "Primary jq differential harness kept aligned with Phase 1 proof needs"
      min_lines: 50
    - path: "jqx_test.mbt"
      provides: "Cross-surface public wrapper tests for fidelity-sensitive behavior"
      min_lines: 20
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/jq_diff.sh"
      via: "CI runs jq differential proof"
      pattern: "jq_diff\\.sh"
    - from: ".github/workflows/ci.yml"
      to: "moon info"
      via: "CI reflects the repo quality gate"
      pattern: "moon info"
    - from: "jqx_test.mbt"
      to: "jqx.mbt"
      via: "top-level MoonBit tests exercise the canonical public runtime API"
      pattern: "run_json_text|run"
---

<objective>
Add cross-surface fidelity and ordering proof, then align CI to the Phase 1 gate.

Purpose: Ensure the reorganized shared core is actually proven by the right automated checks and that CI ends the phase green for the right reasons.
Output: Stronger fidelity/order tests, updated differential proof wiring, and a sharper CI configuration.
</objective>

<execution_context>
@C:/Users/shina/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-core-and-compatibility/01-CONTEXT.md
@.planning/phases/01-shared-core-and-compatibility/01-RESEARCH.md
@.planning/phases/01-shared-core-and-compatibility/01-VALIDATION.md
@.planning/phases/01-shared-core-and-compatibility/01-01-PLAN.md
@.planning/phases/01-shared-core-and-compatibility/01-02-PLAN.md
@AGENTS.md

@core/execute_test.mbt
@core/execute_test_support_test.mbt
@js/jqx_js_test.mbt
@jqx_test.mbt
@scripts/jq_diff.sh
@scripts/jq_diff_native.sh
@.github/workflows/ci.yml
@.github/workflows/release-cli.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fidelity and ordering regression proof</name>
  <files>core/execute_test.mbt, core/execute_test_support_test.mbt, js/jqx_js_test.mbt, jqx_test.mbt</files>
  <action>Introduce or tighten tests that prove JSON-text fidelity, numeric representation preservation, and object-key order behavior through the MoonBit top-level runtime and the JS-target wrapper, with coverage targeted at the cases Phase 1 research identified as highest risk during refactor: large-number text preservation, raw-output-sensitive behavior, and ordering semantics crossing surface boundaries. Do not treat core-only tests as sufficient for this plan.</action>
  <verify>moon test && moon test --target js js</verify>
  <done>Regression tests clearly prove fidelity-sensitive behavior through the MoonBit public API and JS wrapper paths, and fail if a wrapper or refactor reintroduces semantic drift.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile differential harnesses with the refactored core</name>
  <files>scripts/jq_diff.sh, scripts/jq_diff_native.sh</files>
  <action>Update the jq differential and native-exit harnesses as needed so they still provide high-signal CLI-facing proof after the shared-core and corpus changes from Plans 01 and 02. Preserve strict comparison where possible, keep temporary exceptions explicit, and avoid converting the harness into a permissive smoke test for raw output, exit status, or ordering-sensitive behavior.</action>
  <verify>bash ./scripts/jq_diff.sh && bash ./scripts/jq_diff.sh scripts/jq_compat_cases.upstream.json && bash ./scripts/jq_diff_native.sh</verify>
  <done>Differential harnesses accurately reflect the new Phase 1 proof standard and continue to prove CLI parity against jq across maintained and upstream corpora.</done>
</task>

<task type="auto">
  <name>Task 3: Reshape CI to match the Phase 1 gate</name>
  <files>.github/workflows/ci.yml, .github/workflows/release-cli.yml</files>
  <action>Audit the current CI and release-proof jobs against the Phase 1 goals. Add missing proof such as the documented MoonBit gate ordering and top-level MoonBit tests where they matter, remove or simplify jobs that are clearly redundant, and preserve the checks that actually prove jq parity, CLI/JS/MoonBit cross-surface fidelity, TS package health, and release rehearsal readiness. Keep the end state green by the end of the phase even if the path there involved temporary breakage.</action>
  <verify>moon info && moon fmt --check && moon check -d && moon test && moon test --target js js && bash ./scripts/ts_packages.sh verify --frozen-lockfile && bash ./scripts/jq_diff.sh && bash ./scripts/jq_diff.sh scripts/jq_compat_cases.upstream.json && bash ./scripts/jq_diff_native.sh</verify>
  <done>The CI workflow is a sharper proof surface for Phase 1, explicitly covering MoonBit top-level tests, JS-target verification, CLI differential parity, and the repo's documented quality gate.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `moon info` runs successfully
- [ ] `moon fmt --check` succeeds
- [ ] `moon check -d` succeeds
- [ ] `moon test` and `moon test --target js js` succeed
- [ ] `bash ./scripts/ts_packages.sh verify --frozen-lockfile` succeeds
- [ ] `bash ./scripts/jq_diff.sh`, `bash ./scripts/jq_diff.sh scripts/jq_compat_cases.upstream.json`, and `bash ./scripts/jq_diff_native.sh` succeed or only report explicitly documented temporary exceptions
</verification>

<success_criteria>

- All tasks completed
- Fidelity-sensitive behavior has explicit regression proof across MoonBit, JS/TS, and CLI surfaces
- Differential harnesses and CI match the refined Phase 1 gate
- The repo finishes the phase with a green, explainable verification surface
  </success_criteria>

<output>
After completion, create `.planning/phases/01-shared-core-and-compatibility/01-03-SUMMARY.md`
</output>

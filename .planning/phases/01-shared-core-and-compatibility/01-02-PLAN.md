---
phase: 01-shared-core-and-compatibility
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - core/jqx.mbt
  - jqx.mbt
  - jqx_public_types.mbt
  - cmd/main.mbt
  - js/jqx_typed.mbt
  - ts/jqx/src/direct_runtime.ts
autonomous: true
requirements:
  - ARCH-01
  - ARCH-03
  - ARCH-04
user_setup: []
must_haves:
  truths:
    - "Shared jq semantics live in one clearer core boundary that can be reorganized without introducing surface-specific meaning."
    - "CLI, MoonBit, and JS/TS wrappers adapt I/O and API shape, but no longer own compatibility-sensitive rules that belong in the core."
    - "The repository structure after refactor makes the semantic center easier to identify and evolve."
  artifacts:
    - path: "core/jqx.mbt"
      provides: "Primary semantic core entrypoint and shared JSON model"
      min_lines: 100
    - path: "jqx.mbt"
      provides: "MoonBit public wrapper aligned around the refactored core boundary"
      min_lines: 40
    - path: "ts/jqx/src/direct_runtime.ts"
      provides: "JS/TS wrapper over the same shared semantics"
      min_lines: 100
  key_links:
    - from: "jqx.mbt"
      to: "core/jqx.mbt"
      via: "public API wrapper delegates semantics to core"
      pattern: "@core\\."
    - from: "cmd/main.mbt"
      to: "core/jqx.mbt"
      via: "CLI adaptation layer consumes shared Value/parse/execute behavior"
      pattern: "@lib\\."
    - from: "ts/jqx/src/direct_runtime.ts"
      to: "jqx.mbt"
      via: "MoonBit bridge remains the JS/TS semantic source"
      pattern: "moonbit\\."
---

<objective>
Refactor runtime boundaries so the shared semantic core is explicit and surface wrappers stay thin.

Purpose: Make the core easier to evolve, reorganize, and verify without semantic drift across CLI, MoonBit, and JS/TS surfaces.
Output: A clearer shared-core structure and thinner surface layers that no longer own compatibility-sensitive behavior.
</objective>

<execution_context>
@C:/Users/shina/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-core-and-compatibility/01-CONTEXT.md
@.planning/phases/01-shared-core-and-compatibility/01-RESEARCH.md
@.planning/phases/01-shared-core-and-compatibility/01-01-PLAN.md
@AGENTS.md

@core/jqx.mbt
@jqx.mbt
@jqx_public_types.mbt
@cmd/main.mbt
@js/jqx_typed.mbt
@ts/jqx/src/direct_runtime.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the semantic boundary in code</name>
  <files>core/jqx.mbt, jqx.mbt, jqx_public_types.mbt</files>
  <action>Use the compatibility corpus and ledger from Plan 01 as the parity baseline while you identify exactly which value-lane semantics, compatibility-lane semantics, numeric fidelity rules, ordering behavior, and error-shaping logic belong in the shared core. Encode that target boundary by moving or reclassifying MoonBit entrypoints and helper types so wrappers stop exposing or re-implementing core-only behavior.</action>
  <verify>moon check -d && bash ./scripts/jq_diff.sh</verify>
  <done>The target semantic boundary is visible in module/function placement, and jq parity is rechecked before deeper structural moves continue.</done>
</task>

<task type="auto">
  <name>Task 2: Move compatibility-sensitive implementation inward</name>
  <files>core/jqx.mbt, jqx.mbt, jqx_public_types.mbt, js/jqx_typed.mbt</files>
  <action>Apply the concrete module/helper moves implied by Task 1. Centralize parse/compile/run behavior, value/text-lane semantics, and shared JSON rules in the core path, even if that requires renaming modules or changing public/private placement before versioning. Prefer staged moves that keep the repo measurable against jq after each checkpoint.</action>
  <verify>moon test && bash ./scripts/jq_diff.sh</verify>
  <done>Compatibility-sensitive behavior now lives in the shared core path, and wrappers primarily delegate instead of carrying semantic logic.</done>
</task>

<task type="auto">
  <name>Task 3: Retarget CLI and JS/TS wrappers to the new core boundary</name>
  <files>cmd/main.mbt, js/jqx_typed.mbt, ts/jqx/src/direct_runtime.ts</files>
  <action>Update CLI argv/stdin adaptation and JS/TS bridge code to consume the reorganized core boundary without introducing surface-only semantic patches. Temporary wrapper-side adjustments are acceptable while migrating, but the completed plan must leave wrapper responsibilities limited to transport, conversion, packaging, and ergonomic adaptation.</action>
  <verify>moon test && moon test --target js js && bash ./scripts/jq_diff.sh && bash ./scripts/jq_diff_native.sh</verify>
  <done>CLI, MoonBit, and JS/TS surfaces all route through the reorganized core and remain compatible with later Phase 1 fidelity proof work.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `moon check -d` succeeds
- [ ] `moon test` succeeds
- [ ] `moon test --target js js` succeeds
- [ ] `bash ./scripts/jq_diff.sh` succeeds or only reports explicitly documented temporary exceptions
- [ ] `bash ./scripts/jq_diff_native.sh` succeeds or only reports explicitly documented temporary exceptions
</verification>

<success_criteria>

- All tasks completed
- Shared semantic ownership is clearer after refactor
- Surface wrappers are thinner and more obviously non-semantic
- The repository remains compatible with later fidelity and CI proof work
  </success_criteria>

<output>
After completion, create `.planning/phases/01-shared-core-and-compatibility/01-02-SUMMARY.md`
</output>

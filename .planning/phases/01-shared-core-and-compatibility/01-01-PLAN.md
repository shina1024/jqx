---
phase: 01-shared-core-and-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/jq_compat_cases.json
  - scripts/jq_compat_cases.upstream.json
  - scripts/jq_upstream_diff_ledger.md
  - scripts/jq_upstream_ledger.sh
autonomous: true
requirements:
  - COMP-01
  - COMP-02
user_setup: []
must_haves:
  truths:
    - "Maintained compatibility cases and upstream-imported cases are clearly categorized as passing, temporarily excepted, or broken."
    - "Every temporary jq difference remaining after Phase 1 work is visible from automated case metadata and one human-readable ledger covering maintained and upstream exceptions."
    - "Compatibility regressions become easier to detect because case coverage, exception tracking, and case-to-ledger linkage are tighter than before."
  artifacts:
    - path: "scripts/jq_compat_cases.json"
      provides: "Maintained jq compatibility corpus with explicit metadata"
      min_lines: 50
    - path: "scripts/jq_compat_cases.upstream.json"
      provides: "Imported upstream compatibility corpus aligned with current ledger expectations"
      min_lines: 50
    - path: "scripts/jq_upstream_diff_ledger.md"
      provides: "Human-readable explanation of known maintained and upstream differences and removal conditions"
      min_lines: 20
  key_links:
    - from: "scripts/jq_compat_cases.json"
      to: "scripts/jq_diff.sh"
      via: "differential harness consumes maintained cases"
      pattern: "jq_compat_cases\\.json"
    - from: "scripts/jq_compat_cases.json"
      to: "scripts/jq_upstream_diff_ledger.md"
      via: "maintained cases map temporary exceptions to the shared compatibility ledger"
      pattern: "skip_reason|expect_error|expect_status"
    - from: "scripts/jq_compat_cases.upstream.json"
      to: "scripts/jq_upstream_diff_ledger.md"
      via: "upstream cases map to the shared compatibility ledger"
      pattern: "skip_reason|expect_error|expect_status"
---

<objective>
Tighten the compatibility corpus and exception tracking around jq 1.8.1.

Purpose: Make compatibility status measurable and reviewable before deeper refactors change behavior.
Output: Updated maintained/upstream case sets and a durable exception ledger that satisfies the tests-plus-docs requirement.
</objective>

<execution_context>
@C:/Users/shina/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-core-and-compatibility/01-CONTEXT.md
@.planning/phases/01-shared-core-and-compatibility/01-RESEARCH.md
@AGENTS.md

@scripts/jq_compat_cases.json
@scripts/jq_compat_cases.upstream.json
@scripts/jq_upstream_diff_ledger.md
@scripts/jq_upstream_import.sh
@scripts/jq_diff.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit case metadata and exception visibility</name>
  <files>scripts/jq_compat_cases.json, scripts/jq_compat_cases.upstream.json, scripts/jq_upstream_diff_ledger.md</files>
  <action>Review the maintained and upstream-imported jq case sets against the current differential harness. Normalize how temporary exceptions are represented so every remaining difference in either corpus is machine-visible in case metadata and cross-referenced from one human-readable ledger. Avoid broadening skips casually; prefer narrow, removable annotations that preserve strict output and error comparison.</action>
  <verify>bash ./scripts/jq_upstream_ledger.sh --verify && bash ./scripts/jq_upstream_import.sh && git diff --exit-code -- scripts/jq_compat_cases.upstream.json</verify>
  <done>Case metadata is internally consistent, temporary differences are explicit across both corpora, and the ledger explains the remaining exceptions with removal conditions.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten the maintained compatibility corpus</name>
  <files>scripts/jq_compat_cases.json, scripts/jq_diff.sh</files>
  <action>Add or refine maintained jq cases for the behaviors Phase 1 is explicitly responsible for: shared-core semantics, JSON-text fidelity, error/exit handling, and object-order-sensitive behavior. Keep the corpus focused on proving jq parity, not on product expansion. If new maintained cases expose missing differentiation between temporary exceptions and true regressions, update the shared ledger alongside the corpus change.</action>
  <verify>bash ./scripts/jq_diff.sh</verify>
  <done>The maintained corpus exercises the highest-risk parity behaviors for Phase 1 and the differential harness reports explainable outcomes.</done>
</task>

<task type="auto">
  <name>Task 3: Align upstream difference tracking with Phase 1 rules</name>
  <files>scripts/jq_compat_cases.upstream.json, scripts/jq_upstream_diff_ledger.md, scripts/jq_upstream_ledger.sh</files>
  <action>Make the upstream-imported case flow compatible with the new Phase 1 exception policy and keep it aligned with the maintained corpus rules. Ensure the shared ledger can answer: what is still different from jq, which corpus exposes it, why it is still different, and what would make the difference removable. Keep the tracking system connected to existing case files and scripts instead of creating a disconnected side channel.</action>
  <verify>bash ./scripts/jq_upstream_ledger.sh --verify && bash ./scripts/jq_diff.sh scripts/jq_compat_cases.upstream.json</verify>
  <done>Upstream cases can be re-run through the existing harness and every surviving difference in either corpus has a documented explanation plus a removal condition.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash ./scripts/jq_upstream_ledger.sh --verify` succeeds
- [ ] `bash ./scripts/jq_diff.sh` passes or only reports explicitly documented temporary exceptions
- [ ] `bash ./scripts/jq_diff.sh scripts/jq_compat_cases.upstream.json` passes or only reports explicitly documented temporary exceptions
- [ ] Case metadata and ledger language stay narrow, removable, and jq-1.8.1-specific
</verification>

<success_criteria>

- All tasks completed
- Maintained and upstream corpora are tighter and easier to audit
- Temporary compatibility differences are visible from both tests and docs
- No new opaque skips or weakened comparison behavior introduced
  </success_criteria>

<output>
After completion, create `.planning/phases/01-shared-core-and-compatibility/01-01-SUMMARY.md`
</output>

---
phase: 03-js-ts-runtime-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ts/jqx/src/index.ts
  - ts/jqx/src/direct_runtime.ts
  - ts/jqx/test/direct_runtime.test.ts
  - ts/jqx/test/typecheck.ts
  - ts/jqx/README.md
autonomous: true
requirements:
  - TS-01
  - TS-05
user_setup: []
must_haves:
  truths:
    - "JS/TS users see one small canonical root runtime API centered on `parseJson`, `isValidJson`, `compile`, `run`, and `runJsonText`, with query helpers and runtime objects clearly secondary."
    - "The normal quick-start path is `run(filter, input)`; compiled execution is the explicit reuse path rather than a competing first example."
    - "Pre-1.0 cleanup can remove weaker or noisier root export shapes without leaving alias-heavy debt."
  artifacts:
    - path: "ts/jqx/src/index.ts"
      provides: "Root package export contract and runtime-first ordering"
      min_lines: 40
    - path: "ts/jqx/src/direct_runtime.ts"
      provides: "Canonical direct runtime entrypoints and CompiledFilter wrapper"
      min_lines: 80
    - path: "ts/jqx/README.md"
      provides: "JS/TS docs aligned to the runtime-first story"
      min_lines: 60
    - path: "ts/jqx/test/direct_runtime.test.ts"
      provides: "Direct-use behavioral proof for the canonical runtime API"
      min_lines: 60
    - path: "ts/jqx/test/typecheck.ts"
      provides: "Type-level proof for the public runtime entrypoints"
      min_lines: 80
  key_links:
    - from: "ts/jqx/src/index.ts"
      to: "ts/jqx/src/direct_runtime.ts"
      via: "root exports route users through the canonical direct runtime rather than ad hoc helpers"
      pattern: "run|runJsonText|compile|parseJson|isValidJson"
    - from: "ts/jqx/README.md"
      to: "ts/jqx/src/index.ts"
      via: "JS/TS examples teach the runtime-first order exposed by the root package"
      pattern: "run\\(|compile\\(|runJsonText"
    - from: "ts/jqx/test/direct_runtime.test.ts"
      to: "ts/jqx/src/index.ts"
      via: "behavioral tests exercise the root runtime surface, not internal-only modules"
      pattern: "run|compile|parseJson|isValidJson"
---

<objective>
Canonicalize the JS/TS root runtime exports and direct-use API.

Purpose: Make `@shina1024/jqx` feel like one obvious runtime-first package instead of a general utility barrel.
Output: A root package, docs, and public tests that present the canonical direct-use API in the intended order.
</objective>

<execution_context>
@C:/Users/shina/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-js-ts-runtime-surface/03-CONTEXT.md
@.planning/phases/03-js-ts-runtime-surface/03-RESEARCH.md
@.planning/phases/03-js-ts-runtime-surface/03-VALIDATION.md
@AGENTS.md

@ts/jqx/src/index.ts
@ts/jqx/src/direct_runtime.ts
@ts/jqx/README.md
@ts/jqx/test/direct_runtime.test.ts
@ts/jqx/test/typecheck.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and trim the root package runtime contract</name>
  <files>ts/jqx/src/index.ts, ts/jqx/src/direct_runtime.ts</files>
  <action>Review the current root exports against the Phase 3 locked decisions. Keep `parseJson`, `isValidJson`, `compile`, `run`, and `runJsonText` as the obvious top-level runtime operations. Preserve `query`, `queryJsonText`, `runtime`, and `queryRuntime` only if they remain clearly secondary and do not force alias-heavy debt into the public surface.</action>
  <verify>pnpm test</verify>
  <done>The root package reads as a small canonical runtime API with no unnecessary alias carry-over.</done>
</task>

<task type="auto">
  <name>Task 2: Reframe docs and regression proof around the direct runtime on-ramp</name>
  <files>ts/jqx/README.md, ts/jqx/test/direct_runtime.test.ts, ts/jqx/test/typecheck.ts</files>
  <action>Update README examples and public runtime tests so the first story is `run(filter, input)`, with `parseJson` and `isValidJson` as supporting helpers, compiled execution as the reuse path, and the JSON-text lane explicitly positioned for fidelity-sensitive workflows. Keep query helpers present but clearly secondary in examples and assertions.</action>
  <verify>pnpm test</verify>
  <done>Docs and tests reinforce one runtime-first public story instead of competing entrypoint narratives.</done>
</task>

<task type="auto">
  <name>Task 3: Remove surface drift that would block pre-1.0 cleanup</name>
  <files>ts/jqx/src/index.ts, ts/jqx/src/direct_runtime.ts, ts/jqx/test/typecheck.ts</files>
  <action>Make the root export module and direct runtime boundary clean enough that future pre-1.0 cleanup remains possible. Prefer one obvious public spelling per operation, avoid keeping compatibility aliases once the canonical shape is chosen, and ensure the type-level proof matches the surface the README advertises.</action>
  <verify>pnpm test</verify>
  <done>The root JS/TS surface can continue to evolve before 1.0 without carrying forward confusing export debt.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` succeeds
- [ ] `ts/jqx/README.md`, `ts/jqx/src/index.ts`, and `ts/jqx/test/direct_runtime.test.ts` all agree on the canonical runtime-first story
- [ ] Query helpers and runtime objects remain available only as secondary public lanes
- [ ] No alias-heavy root export cleanup debt is introduced
</verification>

<success_criteria>

- All tasks completed
- The root package has one obvious runtime-first story
- Docs teach `run(filter, input)` as the default JS/TS entrypoint
- Public behavioral proof covers the canonical runtime names and ordering
  </success_criteria>

<output>
After completion, create `.planning/phases/03-js-ts-runtime-surface/03-01-SUMMARY.md`
</output>

---
phase: 03-js-ts-runtime-surface
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - ts/jqx/src/direct_runtime.ts
  - ts/jqx/src/bind.ts
  - ts/jqx/src/index.ts
  - ts/jqx/test/direct_runtime.test.ts
  - ts/jqx/test/index.test.ts
  - ts/jqx/test/typecheck.ts
  - ts/jqx/README.md
autonomous: true
requirements:
  - TS-02
  - TS-03
  - TS-05
user_setup: []
must_haves:
  truths:
    - "Compiled filters expose stable `.run(Json)` and `.runJsonText(string)` methods that match the direct runtime's value lane and compatibility lane."
    - "`@shina1024/jqx/bind` is an explicit backend-integration surface with a deliberate scope: either it provides clear compiled-filter parity or it clearly documents that it remains text-runtime-only, but it no longer implies an accidental middle ground."
    - "Shared stringify/parse/error/query-normalization logic is centralized enough that direct and bound runtimes cannot silently drift."
  artifacts:
    - path: "ts/jqx/src/direct_runtime.ts"
      provides: "CompiledFilter behavior and direct runtime lane implementation"
      min_lines: 120
    - path: "ts/jqx/src/bind.ts"
      provides: "Binding helpers and streaming client behavior"
      min_lines: 140
    - path: "ts/jqx/test/direct_runtime.test.ts"
      provides: "Compiled runtime proof across value and text lanes"
      min_lines: 80
    - path: "ts/jqx/test/index.test.ts"
      provides: "Binding helper proof across array and streaming paths"
      min_lines: 120
    - path: "ts/jqx/test/typecheck.ts"
      provides: "Type-level proof for compiled and bound runtime contracts"
      min_lines: 120
  key_links:
    - from: "ts/jqx/src/direct_runtime.ts"
      to: "ts/jqx/src/bind.ts"
      via: "shared lane helpers or equivalent shared behavior keep direct and bound runtimes aligned"
      pattern: "encodeRuntimeInput|decodeRuntimeOutputs|normalizeQueryInput|runJsonText"
    - from: "ts/jqx/src/bind.ts"
      to: "ts/adapter-core/src/index.ts"
      via: "binding helpers continue to use the shared `JqxResult` and `JqxRuntimeError` contracts"
      pattern: "JqxResult|JqxRuntimeError|JqxRuntime|JqxQueryRuntime"
    - from: "ts/jqx/test/index.test.ts"
      to: "ts/jqx/src/bind.ts"
      via: "behavioral tests prove Promise and streaming helper behavior on the `/bind` surface"
      pattern: "bindRuntime|bindQueryRuntime|runJsonTextStream|queryJsonTextStream"
---

<objective>
Align compiled-filter behavior and binding helper surfaces.

Purpose: Keep compiled execution, direct runtime helpers, and `/bind` wrappers on one coherent lane model without duplicated drift-prone logic.
Output: A direct runtime and bind surface that share one public story and one failure model.
</objective>

<execution_context>
@C:/Users/shina/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-js-ts-runtime-surface/03-CONTEXT.md
@.planning/phases/03-js-ts-runtime-surface/03-RESEARCH.md
@.planning/phases/03-js-ts-runtime-surface/03-VALIDATION.md
@.planning/phases/03-js-ts-runtime-surface/03-01-PLAN.md
@AGENTS.md

@ts/jqx/src/direct_runtime.ts
@ts/jqx/src/bind.ts
@ts/jqx/src/index.ts
@ts/jqx/test/direct_runtime.test.ts
@ts/jqx/test/index.test.ts
@ts/jqx/test/typecheck.ts
@ts/jqx/README.md
@ts/adapter-core/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract or consolidate shared lane helpers between direct and bound runtimes</name>
  <files>ts/jqx/src/direct_runtime.ts, ts/jqx/src/bind.ts</files>
  <action>Audit duplicated JSON stringify/parse helpers, runtime error normalization, typed DSL detection, and query normalization across the direct runtime and `/bind`. Extract or otherwise centralize the shared pieces so both surfaces stay aligned to the same value-lane and JSON-text semantics.</action>
  <verify>pnpm test</verify>
  <done>Direct and bound runtimes no longer maintain drift-prone copies of the same lane logic.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the compiled-filter versus `/bind` boundary explicitly</name>
  <files>ts/jqx/src/direct_runtime.ts, ts/jqx/src/bind.ts, ts/jqx/src/index.ts, ts/jqx/README.md</files>
  <action>Review `CompiledFilter.run(...)`, `CompiledFilter.runJsonText(...)`, `runtime`, `queryRuntime`, `bindRuntime`, and `bindQueryRuntime` against the locked decisions. Decide whether `/bind` should gain a deliberate compiled-filter story or remain explicitly text-runtime-based for custom backends. In either case, make the boundary clear in types, exports, and docs so users are not left with an accidental or ambiguous middle state.</action>
  <verify>pnpm test</verify>
  <done>Compiled execution and bind helpers read as one coherent JS/TS surface with an explicit scope boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Expand regression proof for compiled, bound, streaming, and typed-query behavior</name>
  <files>ts/jqx/test/direct_runtime.test.ts, ts/jqx/test/index.test.ts, ts/jqx/test/typecheck.ts</files>
  <action>Add or tighten tests so compiled direct execution, bound execution, streaming fallbacks, and query normalization all prove the same lane and failure semantics. Cover both runtime behavior and public type expectations where the direct and bound surfaces connect.</action>
  <verify>pnpm test</verify>
  <done>The repo has regression proof that compiled and bound JS/TS helpers remain aligned to the same public contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` succeeds
- [ ] Compiled execution and `/bind` helpers both reflect the same value-lane and JSON-text lane rules
- [ ] The `/bind` compiled-filter story is explicit rather than accidental or implied
- [ ] Shared helper extraction reduces direct/bind drift risk instead of adding another public abstraction
</verification>

<success_criteria>

- All tasks completed
- Compiled-filter methods are stable and lane-separated
- `/bind` has an explicit, documented scope
- Direct and bound runtimes are regression-protected against semantic drift
  </success_criteria>

<output>
After completion, create `.planning/phases/03-js-ts-runtime-surface/03-02-SUMMARY.md`
</output>

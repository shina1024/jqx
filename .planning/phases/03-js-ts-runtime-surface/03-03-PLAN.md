---
phase: 03-js-ts-runtime-surface
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - ts/jqx/package.json
  - scripts/ts_package_build.mjs
  - scripts/ts_packages.mjs
  - ts/jqx/test/package_exports.test.ts
  - ts/jqx/test/package_typecheck.ts
  - ts/jqx/README.md
autonomous: true
requirements:
  - TS-04
  - TS-05
user_setup: []
must_haves:
  truths:
    - "Documented package entrypoints `@shina1024/jqx` and `@shina1024/jqx/bind` resolve with working ESM, CJS, and TypeScript declaration outputs."
    - "Built artifacts and the package export map stay aligned without manual post-build tweaking."
    - "JS/TS package verification catches artifact and toolchain regressions early enough that pre-1.0 surface cleanup does not silently break consumers."
  artifacts:
    - path: "ts/jqx/package.json"
      provides: "Observable package export contract for runtime and bind entrypoints"
      min_lines: 60
    - path: "scripts/ts_package_build.mjs"
      provides: "ESM/CJS/declaration build pipeline for the npm package"
      min_lines: 60
    - path: "scripts/ts_packages.mjs"
      provides: "Cross-package TS verification orchestration"
      min_lines: 80
    - path: "ts/jqx/test/package_exports.test.ts"
      provides: "Built-artifact smoke tests for package-name imports and requires"
      min_lines: 60
    - path: "ts/jqx/test/package_typecheck.ts"
      provides: "Consumer-facing declaration proof for package entrypoints"
      min_lines: 60
  key_links:
    - from: "ts/jqx/package.json"
      to: "scripts/ts_package_build.mjs"
      via: "the declared export map is reproduced by the actual build outputs"
      pattern: "\"\\.\"|\"./bind\"|\"./zod\"|\"./yup\"|\"./valibot\""
    - from: "scripts/ts_package_build.mjs"
      to: "ts/jqx/test/package_exports.test.ts"
      via: "artifact smoke tests validate the files produced by the build script"
      pattern: "dist|d\\.cts|d\\.ts|cjs|esm"
    - from: "ts/jqx/test/package_typecheck.ts"
      to: "ts/jqx/package.json"
      via: "consumer-facing type proof resolves the package entrypoints declared in the export map"
      pattern: "@shina1024/jqx|@shina1024/jqx/bind"
---

<objective>
Verify package output formats and type declarations.

Purpose: Turn the JS/TS runtime surface into a package that is actually proven as ESM, CJS, and typed output, not just source-level code that looks publishable.
Output: A reliable build/typecheck path plus artifact-level smoke tests for runtime and bind entrypoints.
</objective>

<execution_context>
@C:/Users/shina/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-js-ts-runtime-surface/03-CONTEXT.md
@.planning/phases/03-js-ts-runtime-surface/03-RESEARCH.md
@.planning/phases/03-js-ts-runtime-surface/03-VALIDATION.md
@.planning/phases/03-js-ts-runtime-surface/03-01-PLAN.md
@.planning/phases/03-js-ts-runtime-surface/03-02-PLAN.md
@AGENTS.md

@ts/jqx/package.json
@scripts/ts_package_build.mjs
@scripts/ts_packages.mjs
@ts/jqx/README.md
@ts/jqx/test/direct_runtime.test.ts
@ts/jqx/test/index.test.ts
@ts/jqx/test/typecheck.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the JS package build and typecheck pipeline reliable on the maintained environments</name>
  <files>scripts/ts_package_build.mjs, scripts/ts_packages.mjs, ts/jqx/package.json</files>
  <action>Audit the current `pnpm build` and `pnpm typecheck` failures in `ts/jqx`. Fix the build/typecheck path so it does not depend on fragile OS-specific shim assumptions, or otherwise document and automate the required refresh path. The result should make `pnpm build` and `pnpm typecheck` reliable enough to use as phase verification commands.</action>
  <verify>pnpm build && pnpm typecheck</verify>
  <done>The package can reproduce fresh runtime artifacts and declarations in the maintained local and CI environments.</done>
</task>

<task type="auto">
  <name>Task 2: Add built-artifact smoke tests for runtime and bind entrypoints</name>
  <files>ts/jqx/test/package_exports.test.ts, ts/jqx/test/package_typecheck.ts, ts/jqx/package.json</files>
  <action>After a fresh build, prove that `@shina1024/jqx` and `@shina1024/jqx/bind` work as package-name imports in ESM and CJS contexts and that their declaration outputs resolve for TS consumers. Keep adapter subpaths limited to existence or import-resolution smoke tests here; full adapter behavior remains a later phase concern.</action>
  <verify>pnpm build && pnpm test</verify>
  <done>The published runtime and bind entrypoints are proven as artifacts, not only as source modules.</done>
</task>

<task type="auto">
  <name>Task 3: Fold artifact proof into the documented TS package gate</name>
  <files>scripts/ts_packages.mjs, ts/jqx/package.json, ts/jqx/README.md</files>
  <action>Wire the new artifact and declaration checks into the normal TS verification workflow so package-output regressions are caught by the documented gate. Align README and package scripts with the actual supported verification path, and make sure the export map and the produced files stay in lockstep.</action>
  <verify>bash ./scripts/ts_packages.sh verify --frozen-lockfile</verify>
  <done>TS-04 is proven by the normal package verification workflow instead of by manual spot checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build` succeeds
- [ ] `pnpm typecheck` succeeds
- [ ] `pnpm test` succeeds with built-artifact smoke coverage
- [ ] `bash ./scripts/ts_packages.sh verify --frozen-lockfile` succeeds
- [ ] `@shina1024/jqx` and `@shina1024/jqx/bind` are proven as ESM, CJS, and typed entrypoints
- [ ] Adapter subpaths remain intentional and do not break the package build while deeper adapter hardening stays deferred to Phase 5
</verification>

<success_criteria>

- All tasks completed
- Runtime and bind package entrypoints are artifact-level verified
- The build/typecheck path is reliable enough to use as a normal phase gate
- Package verification catches export-map and declaration regressions before release work
  </success_criteria>

<output>
After completion, create `.planning/phases/03-js-ts-runtime-surface/03-03-SUMMARY.md`
</output>

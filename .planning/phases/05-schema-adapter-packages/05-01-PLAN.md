---
phase: 05-schema-adapter-packages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ts/adapter-core/src/index.ts
  - ts/jqx/package.json
  - ts/jqx/src/zod.ts
  - ts/jqx/src/yup.ts
  - ts/jqx/src/valibot.ts
  - ts/jqx/test/package_exports.test.ts
  - ts/zod-adapter/package.json
  - ts/zod-adapter/test/index.test.ts
  - ts/yup-adapter/package.json
  - ts/yup-adapter/test/index.test.ts
  - ts/valibot-adapter/package.json
  - ts/valibot-adapter/test/index.test.ts
  - scripts/ts_package_build.mjs
autonomous: true
requirements:
  - ADPT-01
  - ADPT-02
  - ADPT-03
user_setup: []
must_haves:
  truths:
    - "`@shina1024/jqx` is a runtime-only package surface with no `./zod`, `./yup`, or `./valibot` export entries."
    - "Standalone adapter packages remain the only canonical JS/TS adapter import path and still execute through the shared `JqxRuntime` contract."
    - "Package-boundary changes are proven with built-artifact or package-name checks rather than source-file-only tests."
  artifacts:
    - path: "ts/jqx/package.json"
      provides: "Root npm export map and dependency boundary"
      min_lines: 70
    - path: "ts/jqx/test/package_exports.test.ts"
      provides: "Built-artifact proof that root exports remain runtime-only"
      min_lines: 50
    - path: "ts/adapter-core/src/index.ts"
      provides: "Shared adapter contract types and runtime helpers"
      min_lines: 350
    - path: "ts/zod-adapter/test/index.test.ts"
      provides: "Standalone Zod package behavior and package-boundary proof"
      min_lines: 20
    - path: "ts/yup-adapter/test/index.test.ts"
      provides: "Standalone Yup package behavior and package-boundary proof"
      min_lines: 30
    - path: "ts/valibot-adapter/test/index.test.ts"
      provides: "Standalone Valibot package behavior and package-boundary proof"
      min_lines: 20
  key_links:
    - from: "ts/jqx/package.json"
      to: "ts/jqx/test/package_exports.test.ts"
      via: "Root package exports and tests must agree that only `.` and `./bind` are public root entrypoints."
      pattern: "\\./bind|\\./zod|\\./yup|\\./valibot"
    - from: "ts/adapter-core/src/index.ts"
      to: "ts/zod-adapter/src/index.ts"
      via: "Zod adapter remains a consumer of the shared `CoreAdapterError` and runtime-validation helpers."
      pattern: "CoreAdapterError|runFilterWithValidation|runQueryWithValidation"
    - from: "ts/adapter-core/src/index.ts"
      to: "ts/yup-adapter/src/index.ts"
      via: "Yup adapter remains on the same runtime contract rather than adding package-local runtime semantics."
      pattern: "CoreAdapterError|runFilterWithValidation|runQueryWithValidation"
    - from: "ts/adapter-core/src/index.ts"
      to: "ts/valibot-adapter/src/index.ts"
      via: "Valibot adapter remains on the same runtime contract rather than adding package-local runtime semantics."
      pattern: "CoreAdapterError|runFilterWithValidation|runQueryWithValidation"
---

<objective>
Make the runtime package and adapter packages tell one coherent package-boundary story.

Purpose: Remove root-package adapter subpaths, keep adapter-core as the shared internal contract, and restore package-level proof so Phase 5 can harden standalone adapters without a split public surface.
Output: Root export cleanup, standalone-package proof, and a verification path that catches broken package artifacts instead of only source imports.
</objective>

<execution_context>
@C:/Users/shina/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-schema-adapter-packages/05-CONTEXT.md
@.planning/phases/05-schema-adapter-packages/05-RESEARCH.md
@.planning/phases/05-schema-adapter-packages/05-VALIDATION.md
@.planning/phases/03-js-ts-runtime-surface/03-CONTEXT.md
@AGENTS.md

@ts/adapter-core/src/index.ts
@ts/adapter-core/test/adapter_contract_cases.ts
@ts/jqx/package.json
@ts/jqx/src/index.ts
@ts/jqx/src/zod.ts
@ts/jqx/src/yup.ts
@ts/jqx/src/valibot.ts
@ts/jqx/test/package_exports.test.ts
@ts/jqx/test/direct_runtime.test.ts
@ts/zod-adapter/package.json
@ts/zod-adapter/src/index.ts
@ts/zod-adapter/test/index.test.ts
@ts/yup-adapter/package.json
@ts/yup-adapter/src/index.ts
@ts/yup-adapter/test/index.test.ts
@ts/valibot-adapter/package.json
@ts/valibot-adapter/src/index.ts
@ts/valibot-adapter/test/index.test.ts
@scripts/ts_package_build.mjs
@scripts/ts_packages.ps1
@scripts/ts_packages.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove root adapter subpaths and make `@shina1024/jqx` runtime-only</name>
  <files>ts/jqx/package.json, ts/jqx/src/zod.ts, ts/jqx/src/yup.ts, ts/jqx/src/valibot.ts, ts/jqx/test/package_exports.test.ts</files>
  <read_first>ts/jqx/package.json, ts/jqx/src/index.ts, ts/jqx/src/zod.ts, ts/jqx/src/yup.ts, ts/jqx/src/valibot.ts, ts/jqx/test/package_exports.test.ts, ts/jqx/test/direct_runtime.test.ts, .planning/phases/05-schema-adapter-packages/05-CONTEXT.md, .planning/phases/05-schema-adapter-packages/05-RESEARCH.md, AGENTS.md</read_first>
  <action>Delete the `./zod`, `./yup`, and `./valibot` entries from `ts/jqx/package.json` `exports`. Remove the matching adapter shim build inputs from the `build` script so `src/zod.ts`, `src/yup.ts`, and `src/valibot.ts` are no longer emitted as root-package artifacts. Delete those shim source files if they are no longer needed. Remove root-package adapter runtime dependencies and local file overrides from `ts/jqx/package.json` if no remaining root-package code or tests require them. Rewrite `ts/jqx/test/package_exports.test.ts` so it still proves `.` and `./bind` resolve, but explicitly asserts the root export map no longer defines `./zod`, `./yup`, or `./valibot` and no test imports those removed subpaths.</action>
  <verify>pnpm build && pnpm test</verify>
  <acceptance_criteria>
    - `ts/jqx/package.json` does not contain `"./zod"`
    - `ts/jqx/package.json` does not contain `"./yup"`
    - `ts/jqx/package.json` does not contain `"./valibot"`
    - `ts/jqx/package.json` does not contain `src/zod.ts`
    - `ts/jqx/package.json` does not contain `src/yup.ts`
    - `ts/jqx/package.json` does not contain `src/valibot.ts`
    - `ts/jqx/package.json` does not contain `@shina1024/jqx-zod-adapter`
    - `ts/jqx/package.json` does not contain `@shina1024/jqx-yup-adapter`
    - `ts/jqx/package.json` does not contain `@shina1024/jqx-valibot-adapter`
    - `ts/jqx/test/package_exports.test.ts` contains `package-name ESM imports resolve root and bind entrypoints`
    - `ts/jqx/test/package_exports.test.ts` does not contain `@shina1024/jqx/zod`
    - `ts/jqx/test/package_exports.test.ts` does not contain `@shina1024/jqx/yup`
    - `ts/jqx/test/package_exports.test.ts` does not contain `@shina1024/jqx/valibot`
    - `pnpm build && pnpm test` exits 0 in `ts/jqx`
  </acceptance_criteria>
  <done>The root package is runtime-only and no longer publishes adapter subpaths.</done>
</task>

<task type="auto">
  <name>Task 2: Prove standalone adapter packages and shared contract boundaries after cleanup</name>
  <files>ts/adapter-core/src/index.ts, ts/zod-adapter/package.json, ts/zod-adapter/test/index.test.ts, ts/yup-adapter/package.json, ts/yup-adapter/test/index.test.ts, ts/valibot-adapter/package.json, ts/valibot-adapter/test/index.test.ts, scripts/ts_package_build.mjs</files>
  <read_first>ts/adapter-core/src/index.ts, ts/adapter-core/test/adapter_contract_cases.ts, ts/zod-adapter/package.json, ts/zod-adapter/src/index.ts, ts/zod-adapter/test/index.test.ts, ts/yup-adapter/package.json, ts/yup-adapter/src/index.ts, ts/yup-adapter/test/index.test.ts, ts/valibot-adapter/package.json, ts/valibot-adapter/src/index.ts, ts/valibot-adapter/test/index.test.ts, .planning/phases/05-schema-adapter-packages/05-RESEARCH.md, .planning/phases/05-schema-adapter-packages/05-VALIDATION.md, scripts/ts_package_build.mjs, scripts/ts_packages.ps1, scripts/ts_packages.sh</read_first>
  <action>Keep `ts/adapter-core/src/index.ts` as the shared source of `JqxRuntime`, `JqxQueryRuntime`, `AdapterError`, `runFilterWithValidation`, `runQueryWithValidation`, and `runInferred`; do not move runtime semantics into package-local adapters. Add or tighten standalone-package proof inside `ts/zod-adapter/test/index.test.ts`, `ts/yup-adapter/test/index.test.ts`, and `ts/valibot-adapter/test/index.test.ts` so each package still proves `createAdapter(...)` and `createQueryAdapter(...)` run through the shared contract after root subpath removal. If `pnpm build && pnpm test` in `ts/jqx` still fails because `ts_package_build.mjs` cannot find its native binding, repair that build path in `scripts/ts_package_build.mjs` or use the documented `./scripts/ts_packages.ps1 refresh` / `bash ./scripts/ts_packages.sh refresh` flow and do not declare this task done until the root built-artifact verify command is green.</action>
  <verify>bash ./scripts/ts_packages.sh verify --frozen-lockfile</verify>
  <acceptance_criteria>
    - `ts/adapter-core/src/index.ts` contains `export type AdapterError`
    - `ts/adapter-core/src/index.ts` contains `export async function runFilterWithValidation`
    - `ts/adapter-core/src/index.ts` contains `export async function runQueryWithValidation`
    - `ts/zod-adapter/test/index.test.ts` contains `createAdapter`
    - `ts/yup-adapter/test/index.test.ts` contains `createAdapter`
    - `ts/valibot-adapter/test/index.test.ts` contains `createAdapter`
    - `pnpm test` exits 0 in `ts/zod-adapter`
    - `pnpm test` exits 0 in `ts/yup-adapter`
    - `pnpm test` exits 0 in `ts/valibot-adapter`
    - `bash ./scripts/ts_packages.sh verify --frozen-lockfile` exits 0
  </acceptance_criteria>
  <done>Standalone adapters and the shared contract are proven after the root-package cleanup.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build && pnpm test` succeeds in `ts/jqx`
- [ ] `pnpm test` succeeds in `ts/zod-adapter`
- [ ] `pnpm test` succeeds in `ts/yup-adapter`
- [ ] `pnpm test` succeeds in `ts/valibot-adapter`
- [ ] `bash ./scripts/ts_packages.sh verify --frozen-lockfile` succeeds
- [ ] Root package no longer publishes or imports adapter subpaths
- [ ] Standalone adapter packages still prove shared-contract behavior
</verification>

<success_criteria>

- All tasks completed
- `@shina1024/jqx` is a runtime-only package surface
- Standalone adapter packages remain the only canonical adapter imports
- Package-boundary cleanup is proven by built-artifact verification rather than source-only smoke
  </success_criteria>

<output>
After completion, create `.planning/phases/05-schema-adapter-packages/05-01-SUMMARY.md`
</output>

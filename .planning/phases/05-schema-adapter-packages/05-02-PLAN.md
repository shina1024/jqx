---
phase: 05-schema-adapter-packages
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - ts/jqx/README.md
  - ts/zod-adapter/README.md
  - ts/yup-adapter/README.md
  - ts/valibot-adapter/README.md
  - ts/zod-adapter/src/index.ts
  - ts/zod-adapter/test/typecheck.ts
  - ts/yup-adapter/src/index.ts
  - ts/yup-adapter/test/index.test.ts
  - ts/yup-adapter/test/typecheck.ts
  - ts/valibot-adapter/src/index.ts
  - ts/valibot-adapter/test/index.test.ts
  - ts/valibot-adapter/test/typecheck.ts
autonomous: true
requirements:
  - ADPT-01
  - ADPT-02
  - ADPT-03
user_setup: []
must_haves:
  truths:
    - "Every adapter README teaches the standalone package name and starts with `createAdapter(runtime).filter(...)` as the primary on-ramp."
    - "Yup and Valibot adapter errors preserve structured validator-native issue detail instead of collapsing to `string[]`."
    - "The stable top-level adapter error contract still uses `input_validation`, `runtime`, and `output_validation` with a jqx-owned top-level `message`."
  artifacts:
    - path: "ts/jqx/README.md"
      provides: "Root package runtime-first docs and adapter package references"
      min_lines: 120
    - path: "ts/zod-adapter/README.md"
      provides: "Canonical Zod adapter install/import/example flow"
      min_lines: 40
    - path: "ts/yup-adapter/src/index.ts"
      provides: "Yup issue preservation and adapter factory contract"
      min_lines: 90
    - path: "ts/yup-adapter/test/typecheck.ts"
      provides: "Yup public type proof for richer adapter errors"
      min_lines: 120
    - path: "ts/valibot-adapter/src/index.ts"
      provides: "Valibot issue preservation and adapter factory contract"
      min_lines: 100
    - path: "ts/valibot-adapter/test/typecheck.ts"
      provides: "Valibot public type proof for richer adapter errors"
      min_lines: 110
  key_links:
    - from: "ts/jqx/README.md"
      to: "ts/zod-adapter/README.md"
      via: "Root docs and adapter docs must agree on standalone package names and runtime injection."
      pattern: "@shina1024/jqx-zod-adapter|createAdapter\\(runtime\\)"
    - from: "ts/yup-adapter/src/index.ts"
      to: "ts/yup-adapter/test/typecheck.ts"
      via: "Yup issue payload changes must stay reflected in public adapter types."
      pattern: "AdapterError|ValidationError|issues"
    - from: "ts/valibot-adapter/src/index.ts"
      to: "ts/valibot-adapter/test/typecheck.ts"
      via: "Valibot issue payload changes must stay reflected in public adapter types."
      pattern: "AdapterError|BaseIssue|issues"
---

<objective>
Make the three adapter packages feel like stable, symmetric public products.

Purpose: Update docs and examples to use canonical standalone imports, keep the runtime-first story clear, and preserve richer native issue data for Yup and Valibot without changing the shared top-level adapter error contract.
Output: Runtime-first README alignment plus stronger adapter type and runtime proof for Zod, Yup, and Valibot.
</objective>

<execution_context>
@C:/Users/shina/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-schema-adapter-packages/05-CONTEXT.md
@.planning/phases/05-schema-adapter-packages/05-RESEARCH.md
@.planning/phases/05-schema-adapter-packages/05-VALIDATION.md
@.planning/phases/05-schema-adapter-packages/05-01-PLAN.md
@AGENTS.md

@ts/jqx/README.md
@ts/zod-adapter/README.md
@ts/zod-adapter/src/index.ts
@ts/zod-adapter/test/index.test.ts
@ts/zod-adapter/test/typecheck.ts
@ts/yup-adapter/README.md
@ts/yup-adapter/src/index.ts
@ts/yup-adapter/test/index.test.ts
@ts/yup-adapter/test/typecheck.ts
@ts/valibot-adapter/README.md
@ts/valibot-adapter/src/index.ts
@ts/valibot-adapter/test/index.test.ts
@ts/valibot-adapter/test/typecheck.ts
@ts/adapter-core/src/index.ts
@ts/adapter-core/test/adapter_contract_cases.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve native Yup and Valibot issue detail while keeping the shared error shape stable</name>
  <files>ts/yup-adapter/src/index.ts, ts/yup-adapter/test/index.test.ts, ts/yup-adapter/test/typecheck.ts, ts/valibot-adapter/src/index.ts, ts/valibot-adapter/test/index.test.ts, ts/valibot-adapter/test/typecheck.ts, ts/zod-adapter/src/index.ts, ts/zod-adapter/test/typecheck.ts</files>
  <read_first>ts/yup-adapter/src/index.ts, ts/yup-adapter/test/index.test.ts, ts/yup-adapter/test/typecheck.ts, ts/valibot-adapter/src/index.ts, ts/valibot-adapter/test/index.test.ts, ts/valibot-adapter/test/typecheck.ts, ts/zod-adapter/src/index.ts, ts/zod-adapter/test/typecheck.ts, ts/adapter-core/src/index.ts, ts/adapter-core/test/adapter_contract_cases.ts, .planning/phases/05-schema-adapter-packages/05-CONTEXT.md, .planning/phases/05-schema-adapter-packages/05-RESEARCH.md</read_first>
  <action>Change the Yup adapter to stop returning `CoreAdapterError<string[]>` and instead use `type AdapterError = CoreAdapterError<yup.ValidationError[]>`. Preserve native Yup issue detail by returning `error.inner` when it is non-empty and `[error]` otherwise, while keeping the top-level adapter error `kind` and `message` unchanged. Change the Valibot adapter to stop returning `CoreAdapterError<string[]>` and instead use `type AdapterError = CoreAdapterError<v.BaseIssue<unknown>[]>`, returning the native `result.issues` array from `safeParseAsync(...)`. Keep Zod on its current native issue path and update its type fixtures only if needed to keep the three adapters symmetric at the top-level contract. Add runtime tests and type expectations that assert Yup and Valibot `issues[0]` expose structured validator-native fields rather than plain strings.</action>
  <verify>pnpm test && pnpm typecheck</verify>
  <acceptance_criteria>
    - `ts/yup-adapter/src/index.ts` does not contain `CoreAdapterError<string[]>`
    - `ts/valibot-adapter/src/index.ts` does not contain `CoreAdapterError<string[]>`
    - `ts/yup-adapter/src/index.ts` contains `type AdapterError = CoreAdapterError<yup.ValidationError[]>`
    - `ts/valibot-adapter/src/index.ts` contains `type AdapterError = CoreAdapterError<v.BaseIssue<unknown>[]>`
    - `ts/yup-adapter/src/index.ts` contains `return error.inner`
    - `ts/valibot-adapter/src/index.ts` contains `issues: result.issues`
    - `ts/yup-adapter/test/index.test.ts` contains `issues[0]`
    - `ts/valibot-adapter/test/index.test.ts` contains `issues[0]`
    - `ts/yup-adapter/test/typecheck.ts` contains `yup.ValidationError[]`
    - `ts/valibot-adapter/test/typecheck.ts` contains `v.BaseIssue<unknown>[]`
    - `pnpm test && pnpm typecheck` exits 0 in `ts/yup-adapter`
    - `pnpm test && pnpm typecheck` exits 0 in `ts/valibot-adapter`
    - `pnpm test && pnpm typecheck` exits 0 in `ts/zod-adapter`
  </acceptance_criteria>
  <done>All three adapters keep one top-level error contract while Yup and Valibot preserve richer native issue detail.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite root and adapter docs around standalone package imports and filter-first examples</name>
  <files>ts/jqx/README.md, ts/zod-adapter/README.md, ts/yup-adapter/README.md, ts/valibot-adapter/README.md</files>
  <read_first>ts/jqx/README.md, ts/zod-adapter/README.md, ts/yup-adapter/README.md, ts/valibot-adapter/README.md, ts/jqx/package.json, ts/zod-adapter/package.json, ts/yup-adapter/package.json, ts/valibot-adapter/package.json, .planning/phases/05-schema-adapter-packages/05-CONTEXT.md, .planning/phases/05-schema-adapter-packages/05-RESEARCH.md</read_first>
  <action>Update `ts/jqx/README.md` so the adapter section names `@shina1024/jqx-zod-adapter`, `@shina1024/jqx-yup-adapter`, and `@shina1024/jqx-valibot-adapter` as the canonical packages and does not mention `@shina1024/jqx/zod`, `@shina1024/jqx/yup`, or `@shina1024/jqx/valibot`. Update each adapter README so its install block explicitly includes `@shina1024/jqx`, the adapter package, and the validator library, and make the first real example `createAdapter(runtime).filter(...)`. Keep `infer(...)` and `createQueryAdapter(runtime).query(...)` in secondary sections after the main filter flow, and explicitly state that adapters sit on the stable jqx runtime contract rather than internal runtime details.</action>
  <verify>bash ./scripts/ts_packages.sh verify --frozen-lockfile</verify>
  <acceptance_criteria>
    - `ts/jqx/README.md` contains `@shina1024/jqx-zod-adapter`
    - `ts/jqx/README.md` contains `@shina1024/jqx-yup-adapter`
    - `ts/jqx/README.md` contains `@shina1024/jqx-valibot-adapter`
    - `ts/jqx/README.md` does not contain `@shina1024/jqx/zod`
    - `ts/jqx/README.md` does not contain `@shina1024/jqx/yup`
    - `ts/jqx/README.md` does not contain `@shina1024/jqx/valibot`
    - `ts/zod-adapter/README.md` contains `createAdapter(runtime).filter`
    - `ts/yup-adapter/README.md` contains `createAdapter(runtime).filter`
    - `ts/valibot-adapter/README.md` contains `createAdapter(runtime).filter`
    - `ts/zod-adapter/README.md` does not contain `@shina1024/jqx/zod`
    - `ts/yup-adapter/README.md` does not contain `@shina1024/jqx/yup`
    - `ts/valibot-adapter/README.md` does not contain `@shina1024/jqx/valibot`
    - `bash ./scripts/ts_packages.sh verify --frozen-lockfile` exits 0
  </acceptance_criteria>
  <done>Docs and examples present one coherent standalone-adapter story on top of the stable runtime.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test && pnpm typecheck` succeeds in `ts/zod-adapter`
- [ ] `pnpm test && pnpm typecheck` succeeds in `ts/yup-adapter`
- [ ] `pnpm test && pnpm typecheck` succeeds in `ts/valibot-adapter`
- [ ] `bash ./scripts/ts_packages.sh verify --frozen-lockfile` succeeds
- [ ] Root and adapter READMEs use standalone package names only
- [ ] Yup and Valibot adapter `issues` payloads are structured, validator-native data rather than `string[]`
</verification>

<success_criteria>

- All tasks completed
- Every adapter package presents the same filter-first runtime-injection story
- Yup and Valibot preserve richer native issue payloads
- Docs, types, and tests all agree on the standalone package boundary
  </success_criteria>

<output>
After completion, create `.planning/phases/05-schema-adapter-packages/05-02-SUMMARY.md`
</output>

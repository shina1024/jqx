---
phase: 04-cli-workflow-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/main_cli.mbt
  - cmd/main.mbt
  - cmd/output.mbt
  - cmd/main_wbtest.mbt
  - README.mbt.md
autonomous: true
requirements:
  - CLI-01
user_setup: []
must_haves:
  truths:
    - "CLI help text and first-step docs present `jqx` as the canonical user-facing command for stdin and one direct input argument."
    - "CLI input normalization and observable output behavior still flow through the shared core rather than surface-local semantic patches."
    - "Direct input, stdin, raw/slurp handling, and partial-output-before-error behavior are regression-protected by fast CLI tests."
  artifacts:
    - path: "cmd/main_cli.mbt"
      provides: "User-facing CLI usage contract plus compile and execute orchestration"
      min_lines: 120
    - path: "cmd/main.mbt"
      provides: "CLI-side stdin and direct-input normalization"
      min_lines: 100
    - path: "cmd/output.mbt"
      provides: "Raw-vs-JSON output formatting contract"
      min_lines: 5
    - path: "cmd/main_wbtest.mbt"
      provides: "Focused CLI workflow and error-path regression proof"
      min_lines: 320
    - path: "README.mbt.md"
      provides: "Public jqx-first CLI quick-start and common-option examples"
      min_lines: 90
  key_links:
    - from: "cmd/main_cli.mbt"
      to: "cmd/main.mbt"
      via: "CLI entrypoint normalizes inputs through `parse_cli_inputs(...)` before executing the filter"
      pattern: "parse_cli_inputs|cli_reads_stdin"
    - from: "cmd/main_cli.mbt"
      to: "shina1024/jqx/core"
      via: "CLI keeps jq semantics in the shared core through compile and execute entrypoints"
      pattern: "@lib\\.compile|@lib\\.execute_for_cli"
    - from: "README.mbt.md"
      to: "cmd/main_cli.mbt"
      via: "Public examples and usage text agree on the `jqx` command identity"
      pattern: "jqx"
    - from: "cmd/main_wbtest.mbt"
      to: "cmd/main_cli.mbt"
      via: "Fast wbtests pin the same jqx-first usage and workflow behavior the CLI exposes"
      pattern: "run_cli|usage_lines|usage_summary_line"
---

<objective>
Normalize CLI input and output behavior around the shared core.

Purpose: Make the user-facing CLI contract obviously `jqx`-first while keeping stdin/direct-input transport behavior in `cmd` and jq semantics in the shared core.
Output: Updated CLI help/docs plus focused regression proof for direct input, stdin, raw/slurp behavior, and observable output ordering.
</objective>

<execution_context>
@C:/Users/shina/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cli-workflow-parity/04-CONTEXT.md
@.planning/phases/04-cli-workflow-parity/04-RESEARCH.md
@.planning/phases/04-cli-workflow-parity/04-VALIDATION.md
@.planning/phases/01-shared-core-and-compatibility/01-02-SUMMARY.md
@.planning/phases/01-shared-core-and-compatibility/01-03-SUMMARY.md
@AGENTS.md

@cmd/main_cli.mbt
@cmd/main.mbt
@cmd/output.mbt
@cmd/main_wbtest.mbt
@README.mbt.md
@.github/workflows/release-cli.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize the public `jqx` usage and example text</name>
  <files>cmd/main_cli.mbt, README.mbt.md</files>
  <read_first>cmd/main_cli.mbt, README.mbt.md, .planning/phases/04-cli-workflow-parity/04-CONTEXT.md, .planning/phases/04-cli-workflow-parity/04-RESEARCH.md, .github/workflows/release-cli.yml</read_first>
  <action>Change `usage_summary_line()` to return `Usage: jqx [options] "<filter>" ["<json>"]`. Update `usage_lines()` so the stdin and direct-input examples are `echo '{"foo": 1}' | jqx ".foo"` and `jqx ".foo" '{"foo": 1}'`. Keep the option list explicit as `-r`, `-R`, `-c`, `-n`, `-s`, `-e`, and `-L <dir>`. Align the CLI quick-start and common-option examples in `README.mbt.md` to the same `jqx`-first wording. Do not present `moon run --target native cmd -- ...` as the public default anywhere in CLI help text.</action>
  <verify>moon test cmd</verify>
  <acceptance_criteria>
    - `cmd/main_cli.mbt` contains `Usage: jqx [options] "<filter>" ["<json>"]`
    - `cmd/main_cli.mbt` does not contain `moon run --target native cmd --`
    - `README.mbt.md` contains `echo '{"foo": 1}' | jqx ".foo"`
    - `README.mbt.md` contains `jqx ".foo" '{"foo": 1}'`
  </acceptance_criteria>
  <done>CLI help text and README both teach `jqx` as the canonical command.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten focused regression proof for direct input and observable output behavior</name>
  <files>cmd/main.mbt, cmd/output.mbt, cmd/main_wbtest.mbt</files>
  <read_first>cmd/main.mbt, cmd/output.mbt, cmd/main_wbtest.mbt, cmd/main_cli.mbt, .planning/phases/04-cli-workflow-parity/04-RESEARCH.md, .planning/phases/01-shared-core-and-compatibility/01-02-SUMMARY.md, .planning/phases/01-shared-core-and-compatibility/01-03-SUMMARY.md</read_first>
  <action>Add or tighten wbtests that pin the already-green workflow contract: direct input text is used when present, stdin remains the default otherwise, `-R -s` preserves raw direct input as one string, raw string output still comes from `format_output(true, @lib.Value::string(...))`, and runtime errors still keep previously produced outputs after the error line. If those tests expose a bug, fix only `cmd/main.mbt` or `cmd/output.mbt` while keeping stream normalization in `cmd`, compile via `@lib.compile(cfg.filter)`, and execution via `@lib.execute_for_cli(compiled, input)`.</action>
  <verify>moon test cmd</verify>
  <acceptance_criteria>
    - `cmd/main_wbtest.mbt` contains `test "run cli usage text uses jqx command"`
    - `cmd/main_wbtest.mbt` contains `test "parse cli inputs direct input overrides stdin"`
    - `cmd/main_wbtest.mbt` contains `test "parse cli inputs raw direct input slurp"`
    - `cmd/main_cli.mbt` contains `let compiled = @lib.compile(cfg.filter)`
    - `cmd/main_cli.mbt` contains `let result = @lib.execute_for_cli(compiled, input)`
    - `moon test cmd` exits 0
  </acceptance_criteria>
  <done>Fast CLI tests pin the jqx-first workflow contract without moving jq semantics out of the shared core.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `moon test cmd` succeeds
- [ ] `cmd/main_cli.mbt` and `README.mbt.md` both present `jqx` as the public command
- [ ] `cmd/main_cli.mbt` still compiles with `@lib.compile(...)` and executes with `@lib.execute_for_cli(...)`
- [ ] Focused wbtests cover direct input, raw/slurp behavior, and partial-output-before-error behavior
</verification>

<success_criteria>

- All tasks completed
- CLI help and docs are `jqx`-first
- Direct input and stdin remain the only public input workflows
- Focused CLI regressions prove the shared-core execution path and observable output behavior
  </success_criteria>

<output>
After completion, create `.planning/phases/04-cli-workflow-parity/04-01-SUMMARY.md`
</output>

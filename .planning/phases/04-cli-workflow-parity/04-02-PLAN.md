---
phase: 04-cli-workflow-parity
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - scripts/jq_compat_cases.json
  - scripts/jq_exit_cases.json
  - scripts/jq_diff.mjs
  - cmd/main_wbtest.mbt
  - cmd/main_native_wbtest.mbt
autonomous: true
requirements:
  - CLI-01
  - CLI-02
user_setup: []
must_haves:
  truths:
    - "Maintained jq differential proof covers stdin and direct-input workflows plus the common option set `-r`, `-R`, `-s`, `-n`, and `-e` without relaxed comparisons."
    - "CLI error-path and exit-status behavior remain jq-compatible in both focused wbtests and maintained differential runs."
    - "Native-run parity proof still reflects the actual packaged CLI story instead of a wrapper-only `moon run` path."
  artifacts:
    - path: "scripts/jq_compat_cases.json"
      provides: "Maintained jq corpus for common CLI workflows and error-path cases"
      min_lines: 1400
    - path: "scripts/jq_exit_cases.json"
      provides: "Exit-status differential corpus for `-e` behavior"
      min_lines: 50
    - path: "scripts/jq_diff.mjs"
      provides: "Native jq-vs-jqx differential runner with strict stdout, stderr, and status comparison"
      min_lines: 500
    - path: "cmd/main_wbtest.mbt"
      provides: "Focused CLI workflow regressions that mirror maintained corpus expectations"
      min_lines: 340
    - path: "cmd/main_native_wbtest.mbt"
      provides: "Native-only module-path and CLI workflow proof"
      min_lines: 15
  key_links:
    - from: "scripts/jq_diff.mjs"
      to: "scripts/jq_compat_cases.json"
      via: "maintained CLI cases drive native jq-vs-jqx proof with exact output and status comparison"
      pattern: "jqx_args|jqx_use_stdin|expect_status|compat_status"
    - from: "scripts/jq_diff.mjs"
      to: "scripts/jq_exit_cases.json"
      via: "the same strict runner verifies `-e` status behavior from the dedicated exit corpus"
      pattern: "expect_status|jqx_use_stdin"
    - from: "cmd/main_native_wbtest.mbt"
      to: "cmd/main_cli.mbt"
      via: "native wbtests pin the same module-path and CLI behavior exercised by the maintained corpus"
      pattern: "run_cli|-L"
---

<objective>
Finish common jq option parity and error-path behavior.

Purpose: Turn the currently green CLI parity surface into explicit, maintained proof for common options, native workflows, and observable error or exit behavior.
Output: Expanded maintained differential cases plus matching fast and native tests that keep CLI parity strict.
</objective>

<execution_context>
@C:/Users/shina/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cli-workflow-parity/04-CONTEXT.md
@.planning/phases/04-cli-workflow-parity/04-RESEARCH.md
@.planning/phases/04-cli-workflow-parity/04-VALIDATION.md
@.planning/phases/04-cli-workflow-parity/04-01-PLAN.md
@AGENTS.md

@scripts/jq_compat_cases.json
@scripts/jq_exit_cases.json
@scripts/jq_diff.mjs
@cmd/main_wbtest.mbt
@cmd/main_native_wbtest.mbt
@cmd/main_cli.mbt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand maintained differential cases for under-specified CLI workflows</name>
  <files>scripts/jq_compat_cases.json, scripts/jq_exit_cases.json</files>
  <read_first>scripts/jq_compat_cases.json, scripts/jq_exit_cases.json, .planning/phases/04-cli-workflow-parity/04-CONTEXT.md, .planning/phases/04-cli-workflow-parity/04-RESEARCH.md, .planning/phases/04-cli-workflow-parity/04-VALIDATION.md</read_first>
  <action>Add maintained jq cases that pin the Phase 4 workflow story more explicitly. Add `cli-direct-input-raw-string-output`, `cli-direct-input-raw-slurp`, `cli-module-search-path-import`, and `cli-runtime-error-keeps-prior-output` to `scripts/jq_compat_cases.json`, using strict jq-vs-jqx comparison with no relaxed snapshots. Extend `scripts/jq_exit_cases.json` only if a missing `-e` behavior is uncovered while adding those cases; keep all new or updated cases as strict pass cases unless a genuinely explicit temporary exception is required and documented.</action>
  <verify>node scripts/jq_diff.mjs && node scripts/jq_diff.mjs scripts/jq_exit_cases.json</verify>
  <acceptance_criteria>
    - `scripts/jq_compat_cases.json` contains `"name": "cli-direct-input-raw-string-output"`
    - `scripts/jq_compat_cases.json` contains `"name": "cli-direct-input-raw-slurp"`
    - `scripts/jq_compat_cases.json` contains `"name": "cli-module-search-path-import"`
    - `scripts/jq_compat_cases.json` contains `"name": "cli-runtime-error-keeps-prior-output"`
    - `node scripts/jq_diff.mjs` exits 0
    - `node scripts/jq_diff.mjs scripts/jq_exit_cases.json` exits 0
  </acceptance_criteria>
  <done>The maintained CLI corpus explicitly covers the common option and error-path behaviors Phase 4 promises.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror maintained parity gaps in fast and native CLI tests without loosening the harness</name>
  <files>cmd/main_wbtest.mbt, cmd/main_native_wbtest.mbt, scripts/jq_diff.mjs</files>
  <read_first>cmd/main_wbtest.mbt, cmd/main_native_wbtest.mbt, scripts/jq_diff.mjs, cmd/main_cli.mbt, scripts/jq_compat_cases.json, scripts/jq_exit_cases.json, .planning/phases/04-cli-workflow-parity/04-RESEARCH.md</read_first>
  <action>Add or tighten wbtests and native wbtests so the fast `moon test cmd` loop catches the same behaviors added to the maintained corpus: direct-input raw output, raw slurp from direct input, `-L` native module loading, and runtime-error output ordering. If running those tests exposes friction in `scripts/jq_diff.mjs`, fix the runner or output classification only in ways that preserve exact stdout, stderr, and exit-status comparison; do not widen skips or weaken normalization rules.</action>
  <verify>moon test cmd && node scripts/jq_diff.mjs && node scripts/jq_diff.mjs scripts/jq_exit_cases.json</verify>
  <acceptance_criteria>
    - `cmd/main_wbtest.mbt` contains `test "run cli direct input raw string parity case"`
    - `cmd/main_wbtest.mbt` contains `test "run cli runtime error parity corpus case"`
    - `cmd/main_native_wbtest.mbt` contains `test "run cli native module path parity case"`
    - `scripts/jq_diff.mjs` contains `const profile = (process.env.JQX_PROFILE ?? "release").toLowerCase()`
    - `moon test cmd` exits 0
    - `node scripts/jq_diff.mjs` exits 0
    - `node scripts/jq_diff.mjs scripts/jq_exit_cases.json` exits 0
  </acceptance_criteria>
  <done>Fast wbtests, native wbtests, and maintained differential runs all prove the same strict CLI parity story.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `moon test cmd` succeeds
- [ ] `node scripts/jq_diff.mjs` succeeds
- [ ] `node scripts/jq_diff.mjs scripts/jq_exit_cases.json` succeeds
- [ ] New CLI cases remain strict jq comparisons with no widened skips or temporary exceptions added casually
- [ ] Focused wbtests and native wbtests mirror the maintained CLI workflows they are meant to protect
</verification>

<success_criteria>

- All tasks completed
- Maintained CLI differential proof explicitly covers the promised Phase 4 option and error-path workflows
- Exit-status parity stays regression-protected through the dedicated exit corpus
- Native and fast CLI tests match the strict maintained differential story
  </success_criteria>

<output>
After completion, create `.planning/phases/04-cli-workflow-parity/04-02-SUMMARY.md`
</output>

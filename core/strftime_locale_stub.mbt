///|
fn strftime_locale_format(
  pattern : String,
  year : Int,
  month : Int,
  day : Int,
  hour : Int,
  minute : Int,
  second : Int,
  day_of_week : Int,
  day_of_year : Int,
) -> String? {
  ignore(
    (pattern, year, month, day, hour, minute, second, day_of_week, day_of_year),
  )
  None
}
